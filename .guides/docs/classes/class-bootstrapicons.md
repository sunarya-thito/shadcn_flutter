---
title: "Class: BootstrapIcons"
description: "A collection of Bootstrap Icons as Flutter IconData."
---

```dart
/// A collection of Bootstrap Icons as Flutter IconData.
///
/// Provides access to the complete Bootstrap Icons font family.
/// Each icon is defined as a static constant [IconData] that can be
/// used directly with Flutter's [Icon] widget.
///
/// Example:
/// ```dart
/// Icon(BootstrapIcons.alarm)
/// ```
class BootstrapIcons {
  /// Bootstrap icon for icon123.
  static const IconData icon123;
  /// Bootstrap icon for alarm fill.
  static const IconData alarmFill;
  /// Bootstrap icon for alarm.
  static const IconData alarm;
  /// Bootstrap icon for align bottom.
  static const IconData alignBottom;
  /// Bootstrap icon for align center.
  static const IconData alignCenter;
  /// Bootstrap icon for align end.
  static const IconData alignEnd;
  /// Bootstrap icon for align middle.
  static const IconData alignMiddle;
  /// Bootstrap icon for align start.
  static const IconData alignStart;
  /// Bootstrap icon for align top.
  static const IconData alignTop;
  /// Bootstrap icon for alt.
  static const IconData alt;
  /// Bootstrap icon for app indicator.
  static const IconData appIndicator;
  /// Bootstrap icon for app.
  static const IconData app;
  /// Bootstrap icon for archive fill.
  static const IconData archiveFill;
  /// Bootstrap icon for archive.
  static const IconData archive;
  /// Bootstrap icon for arrow90deg down.
  static const IconData arrow90degDown;
  /// Bootstrap icon for arrow90deg left.
  static const IconData arrow90degLeft;
  /// Bootstrap icon for arrow90deg right.
  static const IconData arrow90degRight;
  /// Bootstrap icon for arrow90deg up.
  static const IconData arrow90degUp;
  /// Bootstrap icon for arrow bar down.
  static const IconData arrowBarDown;
  /// Bootstrap icon for arrow bar left.
  static const IconData arrowBarLeft;
  /// Bootstrap icon for arrow bar right.
  static const IconData arrowBarRight;
  /// Bootstrap icon for arrow bar up.
  static const IconData arrowBarUp;
  /// Bootstrap icon for arrow clockwise.
  static const IconData arrowClockwise;
  /// Bootstrap icon for arrow counterclockwise.
  static const IconData arrowCounterclockwise;
  /// Bootstrap icon for arrow down circle fill.
  static const IconData arrowDownCircleFill;
  /// Bootstrap icon for arrow down circle.
  static const IconData arrowDownCircle;
  /// Bootstrap icon for arrow down left circle fill.
  static const IconData arrowDownLeftCircleFill;
  /// Bootstrap icon for arrow down left circle.
  static const IconData arrowDownLeftCircle;
  /// Bootstrap icon for arrow down left square fill.
  static const IconData arrowDownLeftSquareFill;
  /// Bootstrap icon for arrow down left square.
  static const IconData arrowDownLeftSquare;
  /// Bootstrap icon for arrow down left.
  static const IconData arrowDownLeft;
  /// Bootstrap icon for arrow down right circle fill.
  static const IconData arrowDownRightCircleFill;
  /// Bootstrap icon for arrow down right circle.
  static const IconData arrowDownRightCircle;
  /// Bootstrap icon for arrow down right square fill.
  static const IconData arrowDownRightSquareFill;
  /// Bootstrap icon for arrow down right square.
  static const IconData arrowDownRightSquare;
  /// Bootstrap icon for arrow down right.
  static const IconData arrowDownRight;
  /// Bootstrap icon for arrow down short.
  static const IconData arrowDownShort;
  /// Bootstrap icon for arrow down square fill.
  static const IconData arrowDownSquareFill;
  /// Bootstrap icon for arrow down square.
  static const IconData arrowDownSquare;
  /// Bootstrap icon for arrow down up.
  static const IconData arrowDownUp;
  /// Bootstrap icon for arrow down.
  static const IconData arrowDown;
  /// Bootstrap icon for arrow left circle fill.
  static const IconData arrowLeftCircleFill;
  /// Bootstrap icon for arrow left circle.
  static const IconData arrowLeftCircle;
  /// Bootstrap icon for arrow left right.
  static const IconData arrowLeftRight;
  /// Bootstrap icon for arrow left short.
  static const IconData arrowLeftShort;
  /// Bootstrap icon for arrow left square fill.
  static const IconData arrowLeftSquareFill;
  /// Bootstrap icon for arrow left square.
  static const IconData arrowLeftSquare;
  /// Bootstrap icon for arrow left.
  static const IconData arrowLeft;
  /// Bootstrap icon for arrow repeat.
  static const IconData arrowRepeat;
  /// Bootstrap icon for arrow return left.
  static const IconData arrowReturnLeft;
  /// Bootstrap icon for arrow return right.
  static const IconData arrowReturnRight;
  /// Bootstrap icon for arrow right circle fill.
  static const IconData arrowRightCircleFill;
  /// Bootstrap icon for arrow right circle.
  static const IconData arrowRightCircle;
  /// Bootstrap icon for arrow right short.
  static const IconData arrowRightShort;
  /// Bootstrap icon for arrow right square fill.
  static const IconData arrowRightSquareFill;
  /// Bootstrap icon for arrow right square.
  static const IconData arrowRightSquare;
  /// Bootstrap icon for arrow right.
  static const IconData arrowRight;
  /// Bootstrap icon for arrow up circle fill.
  static const IconData arrowUpCircleFill;
  /// Bootstrap icon for arrow up circle.
  static const IconData arrowUpCircle;
  /// Bootstrap icon for arrow up left circle fill.
  static const IconData arrowUpLeftCircleFill;
  /// Bootstrap icon for arrow up left circle.
  static const IconData arrowUpLeftCircle;
  /// Bootstrap icon for arrow up left square fill.
  static const IconData arrowUpLeftSquareFill;
  /// Bootstrap icon for arrow up left square.
  static const IconData arrowUpLeftSquare;
  /// Bootstrap icon for arrow up left.
  static const IconData arrowUpLeft;
  /// Bootstrap icon for arrow up right circle fill.
  static const IconData arrowUpRightCircleFill;
  /// Bootstrap icon for arrow up right circle.
  static const IconData arrowUpRightCircle;
  /// Bootstrap icon for arrow up right square fill.
  static const IconData arrowUpRightSquareFill;
  /// Bootstrap icon for arrow up right square.
  static const IconData arrowUpRightSquare;
  /// Bootstrap icon for arrow up right.
  static const IconData arrowUpRight;
  /// Bootstrap icon for arrow up short.
  static const IconData arrowUpShort;
  /// Bootstrap icon for arrow up square fill.
  static const IconData arrowUpSquareFill;
  /// Bootstrap icon for arrow up square.
  static const IconData arrowUpSquare;
  /// Bootstrap icon for arrow up.
  static const IconData arrowUp;
  /// Bootstrap icon for arrows angle contract.
  static const IconData arrowsAngleContract;
  /// Bootstrap icon for arrows angle expand.
  static const IconData arrowsAngleExpand;
  /// Bootstrap icon for arrows collapse.
  static const IconData arrowsCollapse;
  /// Bootstrap icon for arrows expand.
  static const IconData arrowsExpand;
  /// Bootstrap icon for arrows fullscreen.
  static const IconData arrowsFullscreen;
  /// Bootstrap icon for arrows move.
  static const IconData arrowsMove;
  /// Bootstrap icon for aspect ratio fill.
  static const IconData aspectRatioFill;
  /// Bootstrap icon for aspect ratio.
  static const IconData aspectRatio;
  /// Bootstrap icon for asterisk.
  static const IconData asterisk;
  /// Bootstrap icon for at.
  static const IconData at;
  /// Bootstrap icon for award fill.
  static const IconData awardFill;
  /// Bootstrap icon for award.
  static const IconData award;
  /// Bootstrap icon for back.
  static const IconData back;
  /// Bootstrap icon for backspace fill.
  static const IconData backspaceFill;
  /// Bootstrap icon for backspace reverse fill.
  static const IconData backspaceReverseFill;
  /// Bootstrap icon for backspace reverse.
  static const IconData backspaceReverse;
  /// Bootstrap icon for backspace.
  static const IconData backspace;
  /// Bootstrap icon for badge3d fill.
  static const IconData badge3dFill;
  /// Bootstrap icon for badge3d.
  static const IconData badge3d;
  /// Bootstrap icon for badge4k fill.
  static const IconData badge4kFill;
  /// Bootstrap icon for badge4k.
  static const IconData badge4k;
  /// Bootstrap icon for badge8k fill.
  static const IconData badge8kFill;
  /// Bootstrap icon for badge8k.
  static const IconData badge8k;
  /// Bootstrap icon for badge ad fill.
  static const IconData badgeAdFill;
  /// Bootstrap icon for badge ad.
  static const IconData badgeAd;
  /// Bootstrap icon for badge ar fill.
  static const IconData badgeArFill;
  /// Bootstrap icon for badge ar.
  static const IconData badgeAr;
  /// Bootstrap icon for badge cc fill.
  static const IconData badgeCcFill;
  /// Bootstrap icon for badge cc.
  static const IconData badgeCc;
  /// Bootstrap icon for badge hd fill.
  static const IconData badgeHdFill;
  /// Bootstrap icon for badge hd.
  static const IconData badgeHd;
  /// Bootstrap icon for badge tm fill.
  static const IconData badgeTmFill;
  /// Bootstrap icon for badge tm.
  static const IconData badgeTm;
  /// Bootstrap icon for badge vo fill.
  static const IconData badgeVoFill;
  /// Bootstrap icon for badge vo.
  static const IconData badgeVo;
  /// Bootstrap icon for badge vr fill.
  static const IconData badgeVrFill;
  /// Bootstrap icon for badge vr.
  static const IconData badgeVr;
  /// Bootstrap icon for badge wc fill.
  static const IconData badgeWcFill;
  /// Bootstrap icon for badge wc.
  static const IconData badgeWc;
  /// Bootstrap icon for bag check fill.
  static const IconData bagCheckFill;
  /// Bootstrap icon for bag check.
  static const IconData bagCheck;
  /// Bootstrap icon for bag dash fill.
  static const IconData bagDashFill;
  /// Bootstrap icon for bag dash.
  static const IconData bagDash;
  /// Bootstrap icon for bag fill.
  static const IconData bagFill;
  /// Bootstrap icon for bag plus fill.
  static const IconData bagPlusFill;
  /// Bootstrap icon for bag plus.
  static const IconData bagPlus;
  /// Bootstrap icon for bag x fill.
  static const IconData bagXFill;
  /// Bootstrap icon for bag x.
  static const IconData bagX;
  /// Bootstrap icon for bag.
  static const IconData bag;
  /// Bootstrap icon for bar chart fill.
  static const IconData barChartFill;
  /// Bootstrap icon for bar chart line fill.
  static const IconData barChartLineFill;
  /// Bootstrap icon for bar chart line.
  static const IconData barChartLine;
  /// Bootstrap icon for bar chart steps.
  static const IconData barChartSteps;
  /// Bootstrap icon for bar chart.
  static const IconData barChart;
  /// Bootstrap icon for basket fill.
  static const IconData basketFill;
  /// Bootstrap icon for basket.
  static const IconData basket;
  /// Bootstrap icon for basket2 fill.
  static const IconData basket2Fill;
  /// Bootstrap icon for basket2.
  static const IconData basket2;
  /// Bootstrap icon for basket3 fill.
  static const IconData basket3Fill;
  /// Bootstrap icon for basket3.
  static const IconData basket3;
  /// Bootstrap icon for battery charging.
  static const IconData batteryCharging;
  /// Bootstrap icon for battery full.
  static const IconData batteryFull;
  /// Bootstrap icon for battery half.
  static const IconData batteryHalf;
  /// Bootstrap icon for battery.
  static const IconData battery;
  /// Bootstrap icon for bell fill.
  static const IconData bellFill;
  /// Bootstrap icon for bell.
  static const IconData bell;
  /// Bootstrap icon for bezier.
  static const IconData bezier;
  /// Bootstrap icon for bezier2.
  static const IconData bezier2;
  /// Bootstrap icon for bicycle.
  static const IconData bicycle;
  /// Bootstrap icon for binoculars fill.
  static const IconData binocularsFill;
  /// Bootstrap icon for binoculars.
  static const IconData binoculars;
  /// Bootstrap icon for blockquote left.
  static const IconData blockquoteLeft;
  /// Bootstrap icon for blockquote right.
  static const IconData blockquoteRight;
  /// Bootstrap icon for book fill.
  static const IconData bookFill;
  /// Bootstrap icon for book half.
  static const IconData bookHalf;
  /// Bootstrap icon for book.
  static const IconData book;
  /// Bootstrap icon for bookmark check fill.
  static const IconData bookmarkCheckFill;
  /// Bootstrap icon for bookmark check.
  static const IconData bookmarkCheck;
  /// Bootstrap icon for bookmark dash fill.
  static const IconData bookmarkDashFill;
  /// Bootstrap icon for bookmark dash.
  static const IconData bookmarkDash;
  /// Bootstrap icon for bookmark fill.
  static const IconData bookmarkFill;
  /// Bootstrap icon for bookmark heart fill.
  static const IconData bookmarkHeartFill;
  /// Bootstrap icon for bookmark heart.
  static const IconData bookmarkHeart;
  /// Bootstrap icon for bookmark plus fill.
  static const IconData bookmarkPlusFill;
  /// Bootstrap icon for bookmark plus.
  static const IconData bookmarkPlus;
  /// Bootstrap icon for bookmark star fill.
  static const IconData bookmarkStarFill;
  /// Bootstrap icon for bookmark star.
  static const IconData bookmarkStar;
  /// Bootstrap icon for bookmark x fill.
  static const IconData bookmarkXFill;
  /// Bootstrap icon for bookmark x.
  static const IconData bookmarkX;
  /// Bootstrap icon for bookmark.
  static const IconData bookmark;
  /// Bootstrap icon for bookmarks fill.
  static const IconData bookmarksFill;
  /// Bootstrap icon for bookmarks.
  static const IconData bookmarks;
  /// Bootstrap icon for bookshelf.
  static const IconData bookshelf;
  /// Bootstrap icon for bootstrap fill.
  static const IconData bootstrapFill;
  /// Bootstrap icon for bootstrap reboot.
  static const IconData bootstrapReboot;
  /// Bootstrap icon for bootstrap.
  static const IconData bootstrap;
  /// Bootstrap icon for border all.
  static const IconData borderAll;
  /// Bootstrap icon for border bottom.
  static const IconData borderBottom;
  /// Bootstrap icon for border center.
  static const IconData borderCenter;
  /// Bootstrap icon for border inner.
  static const IconData borderInner;
  /// Bootstrap icon for border left.
  static const IconData borderLeft;
  /// Bootstrap icon for border middle.
  static const IconData borderMiddle;
  /// Bootstrap icon for border outer.
  static const IconData borderOuter;
  /// Bootstrap icon for border right.
  static const IconData borderRight;
  /// Bootstrap icon for border style.
  static const IconData borderStyle;
  /// Bootstrap icon for border top.
  static const IconData borderTop;
  /// Bootstrap icon for border width.
  static const IconData borderWidth;
  /// Bootstrap icon for border.
  static const IconData border;
  /// Bootstrap icon for bounding box circles.
  static const IconData boundingBoxCircles;
  /// Bootstrap icon for bounding box.
  static const IconData boundingBox;
  /// Bootstrap icon for box arrow down left.
  static const IconData boxArrowDownLeft;
  /// Bootstrap icon for box arrow down right.
  static const IconData boxArrowDownRight;
  /// Bootstrap icon for box arrow down.
  static const IconData boxArrowDown;
  /// Bootstrap icon for box arrow in down left.
  static const IconData boxArrowInDownLeft;
  /// Bootstrap icon for box arrow in down right.
  static const IconData boxArrowInDownRight;
  /// Bootstrap icon for box arrow in down.
  static const IconData boxArrowInDown;
  /// Bootstrap icon for box arrow in left.
  static const IconData boxArrowInLeft;
  /// Bootstrap icon for box arrow in right.
  static const IconData boxArrowInRight;
  /// Bootstrap icon for box arrow in up left.
  static const IconData boxArrowInUpLeft;
  /// Bootstrap icon for box arrow in up right.
  static const IconData boxArrowInUpRight;
  /// Bootstrap icon for box arrow in up.
  static const IconData boxArrowInUp;
  /// Bootstrap icon for box arrow left.
  static const IconData boxArrowLeft;
  /// Bootstrap icon for box arrow right.
  static const IconData boxArrowRight;
  /// Bootstrap icon for box arrow up left.
  static const IconData boxArrowUpLeft;
  /// Bootstrap icon for box arrow up right.
  static const IconData boxArrowUpRight;
  /// Bootstrap icon for box arrow up.
  static const IconData boxArrowUp;
  /// Bootstrap icon for box seam.
  static const IconData boxSeam;
  /// Bootstrap icon for box.
  static const IconData box;
  /// Bootstrap icon for braces.
  static const IconData braces;
  /// Bootstrap icon for bricks.
  static const IconData bricks;
  /// Bootstrap icon for briefcase fill.
  static const IconData briefcaseFill;
  /// Bootstrap icon for briefcase.
  static const IconData briefcase;
  /// Bootstrap icon for brightness alt high fill.
  static const IconData brightnessAltHighFill;
  /// Bootstrap icon for brightness alt high.
  static const IconData brightnessAltHigh;
  /// Bootstrap icon for brightness alt low fill.
  static const IconData brightnessAltLowFill;
  /// Bootstrap icon for brightness alt low.
  static const IconData brightnessAltLow;
  /// Bootstrap icon for brightness high fill.
  static const IconData brightnessHighFill;
  /// Bootstrap icon for brightness high.
  static const IconData brightnessHigh;
  /// Bootstrap icon for brightness low fill.
  static const IconData brightnessLowFill;
  /// Bootstrap icon for brightness low.
  static const IconData brightnessLow;
  /// Bootstrap icon for broadcast pin.
  static const IconData broadcastPin;
  /// Bootstrap icon for broadcast.
  static const IconData broadcast;
  /// Bootstrap icon for brush fill.
  static const IconData brushFill;
  /// Bootstrap icon for brush.
  static const IconData brush;
  /// Bootstrap icon for bucket fill.
  static const IconData bucketFill;
  /// Bootstrap icon for bucket.
  static const IconData bucket;
  /// Bootstrap icon for bug fill.
  static const IconData bugFill;
  /// Bootstrap icon for bug.
  static const IconData bug;
  /// Bootstrap icon for building.
  static const IconData building;
  /// Bootstrap icon for bullseye.
  static const IconData bullseye;
  /// Bootstrap icon for calculator fill.
  static const IconData calculatorFill;
  /// Bootstrap icon for calculator.
  static const IconData calculator;
  /// Bootstrap icon for calendar check fill.
  static const IconData calendarCheckFill;
  /// Bootstrap icon for calendar check.
  static const IconData calendarCheck;
  /// Bootstrap icon for calendar date fill.
  static const IconData calendarDateFill;
  /// Bootstrap icon for calendar date.
  static const IconData calendarDate;
  /// Bootstrap icon for calendar day fill.
  static const IconData calendarDayFill;
  /// Bootstrap icon for calendar day.
  static const IconData calendarDay;
  /// Bootstrap icon for calendar event fill.
  static const IconData calendarEventFill;
  /// Bootstrap icon for calendar event.
  static const IconData calendarEvent;
  /// Bootstrap icon for calendar fill.
  static const IconData calendarFill;
  /// Bootstrap icon for calendar minus fill.
  static const IconData calendarMinusFill;
  /// Bootstrap icon for calendar minus.
  static const IconData calendarMinus;
  /// Bootstrap icon for calendar month fill.
  static const IconData calendarMonthFill;
  /// Bootstrap icon for calendar month.
  static const IconData calendarMonth;
  /// Bootstrap icon for calendar plus fill.
  static const IconData calendarPlusFill;
  /// Bootstrap icon for calendar plus.
  static const IconData calendarPlus;
  /// Bootstrap icon for calendar range fill.
  static const IconData calendarRangeFill;
  /// Bootstrap icon for calendar range.
  static const IconData calendarRange;
  /// Bootstrap icon for calendar week fill.
  static const IconData calendarWeekFill;
  /// Bootstrap icon for calendar week.
  static const IconData calendarWeek;
  /// Bootstrap icon for calendar x fill.
  static const IconData calendarXFill;
  /// Bootstrap icon for calendar x.
  static const IconData calendarX;
  /// Bootstrap icon for calendar.
  static const IconData calendar;
  /// Bootstrap icon for calendar2 check fill.
  static const IconData calendar2CheckFill;
  /// Bootstrap icon for calendar2 check.
  static const IconData calendar2Check;
  /// Bootstrap icon for calendar2 date fill.
  static const IconData calendar2DateFill;
  /// Bootstrap icon for calendar2 date.
  static const IconData calendar2Date;
  /// Bootstrap icon for calendar2 day fill.
  static const IconData calendar2DayFill;
  /// Bootstrap icon for calendar2 day.
  static const IconData calendar2Day;
  /// Bootstrap icon for calendar2 event fill.
  static const IconData calendar2EventFill;
  /// Bootstrap icon for calendar2 event.
  static const IconData calendar2Event;
  /// Bootstrap icon for calendar2 fill.
  static const IconData calendar2Fill;
  /// Bootstrap icon for calendar2 minus fill.
  static const IconData calendar2MinusFill;
  /// Bootstrap icon for calendar2 minus.
  static const IconData calendar2Minus;
  /// Bootstrap icon for calendar2 month fill.
  static const IconData calendar2MonthFill;
  /// Bootstrap icon for calendar2 month.
  static const IconData calendar2Month;
  /// Bootstrap icon for calendar2 plus fill.
  static const IconData calendar2PlusFill;
  /// Bootstrap icon for calendar2 plus.
  static const IconData calendar2Plus;
  /// Bootstrap icon for calendar2 range fill.
  static const IconData calendar2RangeFill;
  /// Bootstrap icon for calendar2 range.
  static const IconData calendar2Range;
  /// Bootstrap icon for calendar2 week fill.
  static const IconData calendar2WeekFill;
  /// Bootstrap icon for calendar2 week.
  static const IconData calendar2Week;
  /// Bootstrap icon for calendar2 x fill.
  static const IconData calendar2XFill;
  /// Bootstrap icon for calendar2 x.
  static const IconData calendar2X;
  /// Bootstrap icon for calendar2.
  static const IconData calendar2;
  /// Bootstrap icon for calendar3 event fill.
  static const IconData calendar3EventFill;
  /// Bootstrap icon for calendar3 event.
  static const IconData calendar3Event;
  /// Bootstrap icon for calendar3 fill.
  static const IconData calendar3Fill;
  /// Bootstrap icon for calendar3 range fill.
  static const IconData calendar3RangeFill;
  /// Bootstrap icon for calendar3 range.
  static const IconData calendar3Range;
  /// Bootstrap icon for calendar3 week fill.
  static const IconData calendar3WeekFill;
  /// Bootstrap icon for calendar3 week.
  static const IconData calendar3Week;
  /// Bootstrap icon for calendar3.
  static const IconData calendar3;
  /// Bootstrap icon for calendar4 event.
  static const IconData calendar4Event;
  /// Bootstrap icon for calendar4 range.
  static const IconData calendar4Range;
  /// Bootstrap icon for calendar4 week.
  static const IconData calendar4Week;
  /// Bootstrap icon for calendar4.
  static const IconData calendar4;
  /// Bootstrap icon for camera fill.
  static const IconData cameraFill;
  /// Bootstrap icon for camera reels fill.
  static const IconData cameraReelsFill;
  /// Bootstrap icon for camera reels.
  static const IconData cameraReels;
  /// Bootstrap icon for camera video fill.
  static const IconData cameraVideoFill;
  /// Bootstrap icon for camera video off fill.
  static const IconData cameraVideoOffFill;
  /// Bootstrap icon for camera video off.
  static const IconData cameraVideoOff;
  /// Bootstrap icon for camera video.
  static const IconData cameraVideo;
  /// Bootstrap icon for camera.
  static const IconData camera;
  /// Bootstrap icon for camera2.
  static const IconData camera2;
  /// Bootstrap icon for capslock fill.
  static const IconData capslockFill;
  /// Bootstrap icon for capslock.
  static const IconData capslock;
  /// Bootstrap icon for card checklist.
  static const IconData cardChecklist;
  /// Bootstrap icon for card heading.
  static const IconData cardHeading;
  /// Bootstrap icon for card image.
  static const IconData cardImage;
  /// Bootstrap icon for card list.
  static const IconData cardList;
  /// Bootstrap icon for card text.
  static const IconData cardText;
  /// Bootstrap icon for caret down fill.
  static const IconData caretDownFill;
  /// Bootstrap icon for caret down square fill.
  static const IconData caretDownSquareFill;
  /// Bootstrap icon for caret down square.
  static const IconData caretDownSquare;
  /// Bootstrap icon for caret down.
  static const IconData caretDown;
  /// Bootstrap icon for caret left fill.
  static const IconData caretLeftFill;
  /// Bootstrap icon for caret left square fill.
  static const IconData caretLeftSquareFill;
  /// Bootstrap icon for caret left square.
  static const IconData caretLeftSquare;
  /// Bootstrap icon for caret left.
  static const IconData caretLeft;
  /// Bootstrap icon for caret right fill.
  static const IconData caretRightFill;
  /// Bootstrap icon for caret right square fill.
  static const IconData caretRightSquareFill;
  /// Bootstrap icon for caret right square.
  static const IconData caretRightSquare;
  /// Bootstrap icon for caret right.
  static const IconData caretRight;
  /// Bootstrap icon for caret up fill.
  static const IconData caretUpFill;
  /// Bootstrap icon for caret up square fill.
  static const IconData caretUpSquareFill;
  /// Bootstrap icon for caret up square.
  static const IconData caretUpSquare;
  /// Bootstrap icon for caret up.
  static const IconData caretUp;
  /// Bootstrap icon for cart check fill.
  static const IconData cartCheckFill;
  /// Bootstrap icon for cart check.
  static const IconData cartCheck;
  /// Bootstrap icon for cart dash fill.
  static const IconData cartDashFill;
  /// Bootstrap icon for cart dash.
  static const IconData cartDash;
  /// Bootstrap icon for cart fill.
  static const IconData cartFill;
  /// Bootstrap icon for cart plus fill.
  static const IconData cartPlusFill;
  /// Bootstrap icon for cart plus.
  static const IconData cartPlus;
  /// Bootstrap icon for cart x fill.
  static const IconData cartXFill;
  /// Bootstrap icon for cart x.
  static const IconData cartX;
  /// Bootstrap icon for cart.
  static const IconData cart;
  /// Bootstrap icon for cart2.
  static const IconData cart2;
  /// Bootstrap icon for cart3.
  static const IconData cart3;
  /// Bootstrap icon for cart4.
  static const IconData cart4;
  /// Bootstrap icon for cash stack.
  static const IconData cashStack;
  /// Bootstrap icon for cash.
  static const IconData cash;
  /// Bootstrap icon for cast.
  static const IconData cast;
  /// Bootstrap icon for chat dots fill.
  static const IconData chatDotsFill;
  /// Bootstrap icon for chat dots.
  static const IconData chatDots;
  /// Bootstrap icon for chat fill.
  static const IconData chatFill;
  /// Bootstrap icon for chat left dots fill.
  static const IconData chatLeftDotsFill;
  /// Bootstrap icon for chat left dots.
  static const IconData chatLeftDots;
  /// Bootstrap icon for chat left fill.
  static const IconData chatLeftFill;
  /// Bootstrap icon for chat left quote fill.
  static const IconData chatLeftQuoteFill;
  /// Bootstrap icon for chat left quote.
  static const IconData chatLeftQuote;
  /// Bootstrap icon for chat left text fill.
  static const IconData chatLeftTextFill;
  /// Bootstrap icon for chat left text.
  static const IconData chatLeftText;
  /// Bootstrap icon for chat left.
  static const IconData chatLeft;
  /// Bootstrap icon for chat quote fill.
  static const IconData chatQuoteFill;
  /// Bootstrap icon for chat quote.
  static const IconData chatQuote;
  /// Bootstrap icon for chat right dots fill.
  static const IconData chatRightDotsFill;
  /// Bootstrap icon for chat right dots.
  static const IconData chatRightDots;
  /// Bootstrap icon for chat right fill.
  static const IconData chatRightFill;
  /// Bootstrap icon for chat right quote fill.
  static const IconData chatRightQuoteFill;
  /// Bootstrap icon for chat right quote.
  static const IconData chatRightQuote;
  /// Bootstrap icon for chat right text fill.
  static const IconData chatRightTextFill;
  /// Bootstrap icon for chat right text.
  static const IconData chatRightText;
  /// Bootstrap icon for chat right.
  static const IconData chatRight;
  /// Bootstrap icon for chat square dots fill.
  static const IconData chatSquareDotsFill;
  /// Bootstrap icon for chat square dots.
  static const IconData chatSquareDots;
  /// Bootstrap icon for chat square fill.
  static const IconData chatSquareFill;
  /// Bootstrap icon for chat square quote fill.
  static const IconData chatSquareQuoteFill;
  /// Bootstrap icon for chat square quote.
  static const IconData chatSquareQuote;
  /// Bootstrap icon for chat square text fill.
  static const IconData chatSquareTextFill;
  /// Bootstrap icon for chat square text.
  static const IconData chatSquareText;
  /// Bootstrap icon for chat square.
  static const IconData chatSquare;
  /// Bootstrap icon for chat text fill.
  static const IconData chatTextFill;
  /// Bootstrap icon for chat text.
  static const IconData chatText;
  /// Bootstrap icon for chat.
  static const IconData chat;
  /// Bootstrap icon for check all.
  static const IconData checkAll;
  /// Bootstrap icon for check circle fill.
  static const IconData checkCircleFill;
  /// Bootstrap icon for check circle.
  static const IconData checkCircle;
  /// Bootstrap icon for check square fill.
  static const IconData checkSquareFill;
  /// Bootstrap icon for check square.
  static const IconData checkSquare;
  /// Bootstrap icon for check.
  static const IconData check;
  /// Bootstrap icon for check2 all.
  static const IconData check2All;
  /// Bootstrap icon for check2 circle.
  static const IconData check2Circle;
  /// Bootstrap icon for check2 square.
  static const IconData check2Square;
  /// Bootstrap icon for check2.
  static const IconData check2;
  /// Bootstrap icon for chevron bar contract.
  static const IconData chevronBarContract;
  /// Bootstrap icon for chevron bar down.
  static const IconData chevronBarDown;
  /// Bootstrap icon for chevron bar expand.
  static const IconData chevronBarExpand;
  /// Bootstrap icon for chevron bar left.
  static const IconData chevronBarLeft;
  /// Bootstrap icon for chevron bar right.
  static const IconData chevronBarRight;
  /// Bootstrap icon for chevron bar up.
  static const IconData chevronBarUp;
  /// Bootstrap icon for chevron compact down.
  static const IconData chevronCompactDown;
  /// Bootstrap icon for chevron compact left.
  static const IconData chevronCompactLeft;
  /// Bootstrap icon for chevron compact right.
  static const IconData chevronCompactRight;
  /// Bootstrap icon for chevron compact up.
  static const IconData chevronCompactUp;
  /// Bootstrap icon for chevron contract.
  static const IconData chevronContract;
  /// Bootstrap icon for chevron double down.
  static const IconData chevronDoubleDown;
  /// Bootstrap icon for chevron double left.
  static const IconData chevronDoubleLeft;
  /// Bootstrap icon for chevron double right.
  static const IconData chevronDoubleRight;
  /// Bootstrap icon for chevron double up.
  static const IconData chevronDoubleUp;
  /// Bootstrap icon for chevron down.
  static const IconData chevronDown;
  /// Bootstrap icon for chevron expand.
  static const IconData chevronExpand;
  /// Bootstrap icon for chevron left.
  static const IconData chevronLeft;
  /// Bootstrap icon for chevron right.
  static const IconData chevronRight;
  /// Bootstrap icon for chevron up.
  static const IconData chevronUp;
  /// Bootstrap icon for circle fill.
  static const IconData circleFill;
  /// Bootstrap icon for circle half.
  static const IconData circleHalf;
  /// Bootstrap icon for circle square.
  static const IconData circleSquare;
  /// Bootstrap icon for circle.
  static const IconData circle;
  /// Bootstrap icon for clipboard check.
  static const IconData clipboardCheck;
  /// Bootstrap icon for clipboard data.
  static const IconData clipboardData;
  /// Bootstrap icon for clipboard minus.
  static const IconData clipboardMinus;
  /// Bootstrap icon for clipboard plus.
  static const IconData clipboardPlus;
  /// Bootstrap icon for clipboard x.
  static const IconData clipboardX;
  /// Bootstrap icon for clipboard.
  static const IconData clipboard;
  /// Bootstrap icon for clock fill.
  static const IconData clockFill;
  /// Bootstrap icon for clock history.
  static const IconData clockHistory;
  /// Bootstrap icon for clock.
  static const IconData clock;
  /// Bootstrap icon for cloud arrow down fill.
  static const IconData cloudArrowDownFill;
  /// Bootstrap icon for cloud arrow down.
  static const IconData cloudArrowDown;
  /// Bootstrap icon for cloud arrow up fill.
  static const IconData cloudArrowUpFill;
  /// Bootstrap icon for cloud arrow up.
  static const IconData cloudArrowUp;
  /// Bootstrap icon for cloud check fill.
  static const IconData cloudCheckFill;
  /// Bootstrap icon for cloud check.
  static const IconData cloudCheck;
  /// Bootstrap icon for cloud download fill.
  static const IconData cloudDownloadFill;
  /// Bootstrap icon for cloud download.
  static const IconData cloudDownload;
  /// Bootstrap icon for cloud drizzle fill.
  static const IconData cloudDrizzleFill;
  /// Bootstrap icon for cloud drizzle.
  static const IconData cloudDrizzle;
  /// Bootstrap icon for cloud fill.
  static const IconData cloudFill;
  /// Bootstrap icon for cloud fog fill.
  static const IconData cloudFogFill;
  /// Bootstrap icon for cloud fog.
  static const IconData cloudFog;
  /// Bootstrap icon for cloud fog2 fill.
  static const IconData cloudFog2Fill;
  /// Bootstrap icon for cloud fog2.
  static const IconData cloudFog2;
  /// Bootstrap icon for cloud hail fill.
  static const IconData cloudHailFill;
  /// Bootstrap icon for cloud hail.
  static const IconData cloudHail;
  /// Bootstrap icon for cloud haze fill.
  static const IconData cloudHazeFill;
  /// Bootstrap icon for cloud haze.
  static const IconData cloudHaze;
  /// Bootstrap icon for cloud haze2 fill.
  static const IconData cloudHaze2Fill;
  /// Bootstrap icon for cloud lightning fill.
  static const IconData cloudLightningFill;
  /// Bootstrap icon for cloud lightning rain fill.
  static const IconData cloudLightningRainFill;
  /// Bootstrap icon for cloud lightning rain.
  static const IconData cloudLightningRain;
  /// Bootstrap icon for cloud lightning.
  static const IconData cloudLightning;
  /// Bootstrap icon for cloud minus fill.
  static const IconData cloudMinusFill;
  /// Bootstrap icon for cloud minus.
  static const IconData cloudMinus;
  /// Bootstrap icon for cloud moon fill.
  static const IconData cloudMoonFill;
  /// Bootstrap icon for cloud moon.
  static const IconData cloudMoon;
  /// Bootstrap icon for cloud plus fill.
  static const IconData cloudPlusFill;
  /// Bootstrap icon for cloud plus.
  static const IconData cloudPlus;
  /// Bootstrap icon for cloud rain fill.
  static const IconData cloudRainFill;
  /// Bootstrap icon for cloud rain heavy fill.
  static const IconData cloudRainHeavyFill;
  /// Bootstrap icon for cloud rain heavy.
  static const IconData cloudRainHeavy;
  /// Bootstrap icon for cloud rain.
  static const IconData cloudRain;
  /// Bootstrap icon for cloud slash fill.
  static const IconData cloudSlashFill;
  /// Bootstrap icon for cloud slash.
  static const IconData cloudSlash;
  /// Bootstrap icon for cloud sleet fill.
  static const IconData cloudSleetFill;
  /// Bootstrap icon for cloud sleet.
  static const IconData cloudSleet;
  /// Bootstrap icon for cloud snow fill.
  static const IconData cloudSnowFill;
  /// Bootstrap icon for cloud snow.
  static const IconData cloudSnow;
  /// Bootstrap icon for cloud sun fill.
  static const IconData cloudSunFill;
  /// Bootstrap icon for cloud sun.
  static const IconData cloudSun;
  /// Bootstrap icon for cloud upload fill.
  static const IconData cloudUploadFill;
  /// Bootstrap icon for cloud upload.
  static const IconData cloudUpload;
  /// Bootstrap icon for cloud.
  static const IconData cloud;
  /// Bootstrap icon for clouds fill.
  static const IconData cloudsFill;
  /// Bootstrap icon for clouds.
  static const IconData clouds;
  /// Bootstrap icon for cloudy fill.
  static const IconData cloudyFill;
  /// Bootstrap icon for cloudy.
  static const IconData cloudy;
  /// Bootstrap icon for code slash.
  static const IconData codeSlash;
  /// Bootstrap icon for code square.
  static const IconData codeSquare;
  /// Bootstrap icon for code.
  static const IconData code;
  /// Bootstrap icon for collection fill.
  static const IconData collectionFill;
  /// Bootstrap icon for collection play fill.
  static const IconData collectionPlayFill;
  /// Bootstrap icon for collection play.
  static const IconData collectionPlay;
  /// Bootstrap icon for collection.
  static const IconData collection;
  /// Bootstrap icon for columns gap.
  static const IconData columnsGap;
  /// Bootstrap icon for columns.
  static const IconData columns;
  /// Bootstrap icon for command.
  static const IconData command;
  /// Bootstrap icon for compass fill.
  static const IconData compassFill;
  /// Bootstrap icon for compass.
  static const IconData compass;
  /// Bootstrap icon for cone striped.
  static const IconData coneStriped;
  /// Bootstrap icon for cone.
  static const IconData cone;
  /// Bootstrap icon for controller.
  static const IconData controller;
  /// Bootstrap icon for cpu fill.
  static const IconData cpuFill;
  /// Bootstrap icon for cpu.
  static const IconData cpu;
  /// Bootstrap icon for credit card2 back fill.
  static const IconData creditCard2BackFill;
  /// Bootstrap icon for credit card2 back.
  static const IconData creditCard2Back;
  /// Bootstrap icon for credit card2 front fill.
  static const IconData creditCard2FrontFill;
  /// Bootstrap icon for credit card2 front.
  static const IconData creditCard2Front;
  /// Bootstrap icon for credit card fill.
  static const IconData creditCardFill;
  /// Bootstrap icon for credit card.
  static const IconData creditCard;
  /// Bootstrap icon for crop.
  static const IconData crop;
  /// Bootstrap icon for cup fill.
  static const IconData cupFill;
  /// Bootstrap icon for cup straw.
  static const IconData cupStraw;
  /// Bootstrap icon for cup.
  static const IconData cup;
  /// Bootstrap icon for cursor fill.
  static const IconData cursorFill;
  /// Bootstrap icon for cursor text.
  static const IconData cursorText;
  /// Bootstrap icon for cursor.
  static const IconData cursor;
  /// Bootstrap icon for dash circle dotted.
  static const IconData dashCircleDotted;
  /// Bootstrap icon for dash circle fill.
  static const IconData dashCircleFill;
  /// Bootstrap icon for dash circle.
  static const IconData dashCircle;
  /// Bootstrap icon for dash square dotted.
  static const IconData dashSquareDotted;
  /// Bootstrap icon for dash square fill.
  static const IconData dashSquareFill;
  /// Bootstrap icon for dash square.
  static const IconData dashSquare;
  /// Bootstrap icon for dash.
  static const IconData dash;
  /// Bootstrap icon for diagram2 fill.
  static const IconData diagram2Fill;
  /// Bootstrap icon for diagram2.
  static const IconData diagram2;
  /// Bootstrap icon for diagram3 fill.
  static const IconData diagram3Fill;
  /// Bootstrap icon for diagram3.
  static const IconData diagram3;
  /// Bootstrap icon for diamond fill.
  static const IconData diamondFill;
  /// Bootstrap icon for diamond half.
  static const IconData diamondHalf;
  /// Bootstrap icon for diamond.
  static const IconData diamond;
  /// Bootstrap icon for dice1 fill.
  static const IconData dice1Fill;
  /// Bootstrap icon for dice1.
  static const IconData dice1;
  /// Bootstrap icon for dice2 fill.
  static const IconData dice2Fill;
  /// Bootstrap icon for dice2.
  static const IconData dice2;
  /// Bootstrap icon for dice3 fill.
  static const IconData dice3Fill;
  /// Bootstrap icon for dice3.
  static const IconData dice3;
  /// Bootstrap icon for dice4 fill.
  static const IconData dice4Fill;
  /// Bootstrap icon for dice4.
  static const IconData dice4;
  /// Bootstrap icon for dice5 fill.
  static const IconData dice5Fill;
  /// Bootstrap icon for dice5.
  static const IconData dice5;
  /// Bootstrap icon for dice6 fill.
  static const IconData dice6Fill;
  /// Bootstrap icon for dice6.
  static const IconData dice6;
  /// Bootstrap icon for disc fill.
  static const IconData discFill;
  /// Bootstrap icon for disc.
  static const IconData disc;
  /// Bootstrap icon for discord.
  static const IconData discord;
  /// Bootstrap icon for display fill.
  static const IconData displayFill;
  /// Bootstrap icon for display.
  static const IconData display;
  /// Bootstrap icon for distribute horizontal.
  static const IconData distributeHorizontal;
  /// Bootstrap icon for distribute vertical.
  static const IconData distributeVertical;
  /// Bootstrap icon for door closed fill.
  static const IconData doorClosedFill;
  /// Bootstrap icon for door closed.
  static const IconData doorClosed;
  /// Bootstrap icon for door open fill.
  static const IconData doorOpenFill;
  /// Bootstrap icon for door open.
  static const IconData doorOpen;
  /// Bootstrap icon for dot.
  static const IconData dot;
  /// Bootstrap icon for download.
  static const IconData download;
  /// Bootstrap icon for droplet fill.
  static const IconData dropletFill;
  /// Bootstrap icon for droplet half.
  static const IconData dropletHalf;
  /// Bootstrap icon for droplet.
  static const IconData droplet;
  /// Bootstrap icon for earbuds.
  static const IconData earbuds;
  /// Bootstrap icon for easel fill.
  static const IconData easelFill;
  /// Bootstrap icon for easel.
  static const IconData easel;
  /// Bootstrap icon for egg fill.
  static const IconData eggFill;
  /// Bootstrap icon for egg fried.
  static const IconData eggFried;
  /// Bootstrap icon for egg.
  static const IconData egg;
  /// Bootstrap icon for eject fill.
  static const IconData ejectFill;
  /// Bootstrap icon for eject.
  static const IconData eject;
  /// Bootstrap icon for emoji angry fill.
  static const IconData emojiAngryFill;
  /// Bootstrap icon for emoji angry.
  static const IconData emojiAngry;
  /// Bootstrap icon for emoji dizzy fill.
  static const IconData emojiDizzyFill;
  /// Bootstrap icon for emoji dizzy.
  static const IconData emojiDizzy;
  /// Bootstrap icon for emoji expressionless fill.
  static const IconData emojiExpressionlessFill;
  /// Bootstrap icon for emoji expressionless.
  static const IconData emojiExpressionless;
  /// Bootstrap icon for emoji frown fill.
  static const IconData emojiFrownFill;
  /// Bootstrap icon for emoji frown.
  static const IconData emojiFrown;
  /// Bootstrap icon for emoji heart eyes fill.
  static const IconData emojiHeartEyesFill;
  /// Bootstrap icon for emoji heart eyes.
  static const IconData emojiHeartEyes;
  /// Bootstrap icon for emoji laughing fill.
  static const IconData emojiLaughingFill;
  /// Bootstrap icon for emoji laughing.
  static const IconData emojiLaughing;
  /// Bootstrap icon for emoji neutral fill.
  static const IconData emojiNeutralFill;
  /// Bootstrap icon for emoji neutral.
  static const IconData emojiNeutral;
  /// Bootstrap icon for emoji smile fill.
  static const IconData emojiSmileFill;
  /// Bootstrap icon for emoji smile upside down fill.
  static const IconData emojiSmileUpsideDownFill;
  /// Bootstrap icon for emoji smile upside down.
  static const IconData emojiSmileUpsideDown;
  /// Bootstrap icon for emoji smile.
  static const IconData emojiSmile;
  /// Bootstrap icon for emoji sunglasses fill.
  static const IconData emojiSunglassesFill;
  /// Bootstrap icon for emoji sunglasses.
  static const IconData emojiSunglasses;
  /// Bootstrap icon for emoji wink fill.
  static const IconData emojiWinkFill;
  /// Bootstrap icon for emoji wink.
  static const IconData emojiWink;
  /// Bootstrap icon for envelope fill.
  static const IconData envelopeFill;
  /// Bootstrap icon for envelope open fill.
  static const IconData envelopeOpenFill;
  /// Bootstrap icon for envelope open.
  static const IconData envelopeOpen;
  /// Bootstrap icon for envelope.
  static const IconData envelope;
  /// Bootstrap icon for eraser fill.
  static const IconData eraserFill;
  /// Bootstrap icon for eraser.
  static const IconData eraser;
  /// Bootstrap icon for exclamation circle fill.
  static const IconData exclamationCircleFill;
  /// Bootstrap icon for exclamation circle.
  static const IconData exclamationCircle;
  /// Bootstrap icon for exclamation diamond fill.
  static const IconData exclamationDiamondFill;
  /// Bootstrap icon for exclamation diamond.
  static const IconData exclamationDiamond;
  /// Bootstrap icon for exclamation octagon fill.
  static const IconData exclamationOctagonFill;
  /// Bootstrap icon for exclamation octagon.
  static const IconData exclamationOctagon;
  /// Bootstrap icon for exclamation square fill.
  static const IconData exclamationSquareFill;
  /// Bootstrap icon for exclamation square.
  static const IconData exclamationSquare;
  /// Bootstrap icon for exclamation triangle fill.
  static const IconData exclamationTriangleFill;
  /// Bootstrap icon for exclamation triangle.
  static const IconData exclamationTriangle;
  /// Bootstrap icon for exclamation.
  static const IconData exclamation;
  /// Bootstrap icon for exclude.
  static const IconData exclude;
  /// Bootstrap icon for eye fill.
  static const IconData eyeFill;
  /// Bootstrap icon for eye slash fill.
  static const IconData eyeSlashFill;
  /// Bootstrap icon for eye slash.
  static const IconData eyeSlash;
  /// Bootstrap icon for eye.
  static const IconData eye;
  /// Bootstrap icon for eyedropper.
  static const IconData eyedropper;
  /// Bootstrap icon for eyeglasses.
  static const IconData eyeglasses;
  /// Bootstrap icon for facebook.
  static const IconData facebook;
  /// Bootstrap icon for file arrow down fill.
  static const IconData fileArrowDownFill;
  /// Bootstrap icon for file arrow down.
  static const IconData fileArrowDown;
  /// Bootstrap icon for file arrow up fill.
  static const IconData fileArrowUpFill;
  /// Bootstrap icon for file arrow up.
  static const IconData fileArrowUp;
  /// Bootstrap icon for file bar graph fill.
  static const IconData fileBarGraphFill;
  /// Bootstrap icon for file bar graph.
  static const IconData fileBarGraph;
  /// Bootstrap icon for file binary fill.
  static const IconData fileBinaryFill;
  /// Bootstrap icon for file binary.
  static const IconData fileBinary;
  /// Bootstrap icon for file break fill.
  static const IconData fileBreakFill;
  /// Bootstrap icon for file break.
  static const IconData fileBreak;
  /// Bootstrap icon for file check fill.
  static const IconData fileCheckFill;
  /// Bootstrap icon for file check.
  static const IconData fileCheck;
  /// Bootstrap icon for file code fill.
  static const IconData fileCodeFill;
  /// Bootstrap icon for file code.
  static const IconData fileCode;
  /// Bootstrap icon for file diff fill.
  static const IconData fileDiffFill;
  /// Bootstrap icon for file diff.
  static const IconData fileDiff;
  /// Bootstrap icon for file earmark arrow down fill.
  static const IconData fileEarmarkArrowDownFill;
  /// Bootstrap icon for file earmark arrow down.
  static const IconData fileEarmarkArrowDown;
  /// Bootstrap icon for file earmark arrow up fill.
  static const IconData fileEarmarkArrowUpFill;
  /// Bootstrap icon for file earmark arrow up.
  static const IconData fileEarmarkArrowUp;
  /// Bootstrap icon for file earmark bar graph fill.
  static const IconData fileEarmarkBarGraphFill;
  /// Bootstrap icon for file earmark bar graph.
  static const IconData fileEarmarkBarGraph;
  /// Bootstrap icon for file earmark binary fill.
  static const IconData fileEarmarkBinaryFill;
  /// Bootstrap icon for file earmark binary.
  static const IconData fileEarmarkBinary;
  /// Bootstrap icon for file earmark break fill.
  static const IconData fileEarmarkBreakFill;
  /// Bootstrap icon for file earmark break.
  static const IconData fileEarmarkBreak;
  /// Bootstrap icon for file earmark check fill.
  static const IconData fileEarmarkCheckFill;
  /// Bootstrap icon for file earmark check.
  static const IconData fileEarmarkCheck;
  /// Bootstrap icon for file earmark code fill.
  static const IconData fileEarmarkCodeFill;
  /// Bootstrap icon for file earmark code.
  static const IconData fileEarmarkCode;
  /// Bootstrap icon for file earmark diff fill.
  static const IconData fileEarmarkDiffFill;
  /// Bootstrap icon for file earmark diff.
  static const IconData fileEarmarkDiff;
  /// Bootstrap icon for file earmark easel fill.
  static const IconData fileEarmarkEaselFill;
  /// Bootstrap icon for file earmark easel.
  static const IconData fileEarmarkEasel;
  /// Bootstrap icon for file earmark excel fill.
  static const IconData fileEarmarkExcelFill;
  /// Bootstrap icon for file earmark excel.
  static const IconData fileEarmarkExcel;
  /// Bootstrap icon for file earmark fill.
  static const IconData fileEarmarkFill;
  /// Bootstrap icon for file earmark font fill.
  static const IconData fileEarmarkFontFill;
  /// Bootstrap icon for file earmark font.
  static const IconData fileEarmarkFont;
  /// Bootstrap icon for file earmark image fill.
  static const IconData fileEarmarkImageFill;
  /// Bootstrap icon for file earmark image.
  static const IconData fileEarmarkImage;
  /// Bootstrap icon for file earmark lock fill.
  static const IconData fileEarmarkLockFill;
  /// Bootstrap icon for file earmark lock.
  static const IconData fileEarmarkLock;
  /// Bootstrap icon for file earmark lock2 fill.
  static const IconData fileEarmarkLock2Fill;
  /// Bootstrap icon for file earmark lock2.
  static const IconData fileEarmarkLock2;
  /// Bootstrap icon for file earmark medical fill.
  static const IconData fileEarmarkMedicalFill;
  /// Bootstrap icon for file earmark medical.
  static const IconData fileEarmarkMedical;
  /// Bootstrap icon for file earmark minus fill.
  static const IconData fileEarmarkMinusFill;
  /// Bootstrap icon for file earmark minus.
  static const IconData fileEarmarkMinus;
  /// Bootstrap icon for file earmark music fill.
  static const IconData fileEarmarkMusicFill;
  /// Bootstrap icon for file earmark music.
  static const IconData fileEarmarkMusic;
  /// Bootstrap icon for file earmark person fill.
  static const IconData fileEarmarkPersonFill;
  /// Bootstrap icon for file earmark person.
  static const IconData fileEarmarkPerson;
  /// Bootstrap icon for file earmark play fill.
  static const IconData fileEarmarkPlayFill;
  /// Bootstrap icon for file earmark play.
  static const IconData fileEarmarkPlay;
  /// Bootstrap icon for file earmark plus fill.
  static const IconData fileEarmarkPlusFill;
  /// Bootstrap icon for file earmark plus.
  static const IconData fileEarmarkPlus;
  /// Bootstrap icon for file earmark post fill.
  static const IconData fileEarmarkPostFill;
  /// Bootstrap icon for file earmark post.
  static const IconData fileEarmarkPost;
  /// Bootstrap icon for file earmark ppt fill.
  static const IconData fileEarmarkPptFill;
  /// Bootstrap icon for file earmark ppt.
  static const IconData fileEarmarkPpt;
  /// Bootstrap icon for file earmark richtext fill.
  static const IconData fileEarmarkRichtextFill;
  /// Bootstrap icon for file earmark richtext.
  static const IconData fileEarmarkRichtext;
  /// Bootstrap icon for file earmark ruled fill.
  static const IconData fileEarmarkRuledFill;
  /// Bootstrap icon for file earmark ruled.
  static const IconData fileEarmarkRuled;
  /// Bootstrap icon for file earmark slides fill.
  static const IconData fileEarmarkSlidesFill;
  /// Bootstrap icon for file earmark slides.
  static const IconData fileEarmarkSlides;
  /// Bootstrap icon for file earmark spreadsheet fill.
  static const IconData fileEarmarkSpreadsheetFill;
  /// Bootstrap icon for file earmark spreadsheet.
  static const IconData fileEarmarkSpreadsheet;
  /// Bootstrap icon for file earmark text fill.
  static const IconData fileEarmarkTextFill;
  /// Bootstrap icon for file earmark text.
  static const IconData fileEarmarkText;
  /// Bootstrap icon for file earmark word fill.
  static const IconData fileEarmarkWordFill;
  /// Bootstrap icon for file earmark word.
  static const IconData fileEarmarkWord;
  /// Bootstrap icon for file earmark x fill.
  static const IconData fileEarmarkXFill;
  /// Bootstrap icon for file earmark x.
  static const IconData fileEarmarkX;
  /// Bootstrap icon for file earmark zip fill.
  static const IconData fileEarmarkZipFill;
  /// Bootstrap icon for file earmark zip.
  static const IconData fileEarmarkZip;
  /// Bootstrap icon for file earmark.
  static const IconData fileEarmark;
  /// Bootstrap icon for file easel fill.
  static const IconData fileEaselFill;
  /// Bootstrap icon for file easel.
  static const IconData fileEasel;
  /// Bootstrap icon for file excel fill.
  static const IconData fileExcelFill;
  /// Bootstrap icon for file excel.
  static const IconData fileExcel;
  /// Bootstrap icon for file fill.
  static const IconData fileFill;
  /// Bootstrap icon for file font fill.
  static const IconData fileFontFill;
  /// Bootstrap icon for file font.
  static const IconData fileFont;
  /// Bootstrap icon for file image fill.
  static const IconData fileImageFill;
  /// Bootstrap icon for file image.
  static const IconData fileImage;
  /// Bootstrap icon for file lock fill.
  static const IconData fileLockFill;
  /// Bootstrap icon for file lock.
  static const IconData fileLock;
  /// Bootstrap icon for file lock2 fill.
  static const IconData fileLock2Fill;
  /// Bootstrap icon for file lock2.
  static const IconData fileLock2;
  /// Bootstrap icon for file medical fill.
  static const IconData fileMedicalFill;
  /// Bootstrap icon for file medical.
  static const IconData fileMedical;
  /// Bootstrap icon for file minus fill.
  static const IconData fileMinusFill;
  /// Bootstrap icon for file minus.
  static const IconData fileMinus;
  /// Bootstrap icon for file music fill.
  static const IconData fileMusicFill;
  /// Bootstrap icon for file music.
  static const IconData fileMusic;
  /// Bootstrap icon for file person fill.
  static const IconData filePersonFill;
  /// Bootstrap icon for file person.
  static const IconData filePerson;
  /// Bootstrap icon for file play fill.
  static const IconData filePlayFill;
  /// Bootstrap icon for file play.
  static const IconData filePlay;
  /// Bootstrap icon for file plus fill.
  static const IconData filePlusFill;
  /// Bootstrap icon for file plus.
  static const IconData filePlus;
  /// Bootstrap icon for file post fill.
  static const IconData filePostFill;
  /// Bootstrap icon for file post.
  static const IconData filePost;
  /// Bootstrap icon for file ppt fill.
  static const IconData filePptFill;
  /// Bootstrap icon for file ppt.
  static const IconData filePpt;
  /// Bootstrap icon for file richtext fill.
  static const IconData fileRichtextFill;
  /// Bootstrap icon for file richtext.
  static const IconData fileRichtext;
  /// Bootstrap icon for file ruled fill.
  static const IconData fileRuledFill;
  /// Bootstrap icon for file ruled.
  static const IconData fileRuled;
  /// Bootstrap icon for file slides fill.
  static const IconData fileSlidesFill;
  /// Bootstrap icon for file slides.
  static const IconData fileSlides;
  /// Bootstrap icon for file spreadsheet fill.
  static const IconData fileSpreadsheetFill;
  /// Bootstrap icon for file spreadsheet.
  static const IconData fileSpreadsheet;
  /// Bootstrap icon for file text fill.
  static const IconData fileTextFill;
  /// Bootstrap icon for file text.
  static const IconData fileText;
  /// Bootstrap icon for file word fill.
  static const IconData fileWordFill;
  /// Bootstrap icon for file word.
  static const IconData fileWord;
  /// Bootstrap icon for file x fill.
  static const IconData fileXFill;
  /// Bootstrap icon for file x.
  static const IconData fileX;
  /// Bootstrap icon for file zip fill.
  static const IconData fileZipFill;
  /// Bootstrap icon for file zip.
  static const IconData fileZip;
  /// Bootstrap icon for file.
  static const IconData file;
  /// Bootstrap icon for files alt.
  static const IconData filesAlt;
  /// Bootstrap icon for files.
  static const IconData files;
  /// Bootstrap icon for film.
  static const IconData film;
  /// Bootstrap icon for filter circle fill.
  static const IconData filterCircleFill;
  /// Bootstrap icon for filter circle.
  static const IconData filterCircle;
  /// Bootstrap icon for filter left.
  static const IconData filterLeft;
  /// Bootstrap icon for filter right.
  static const IconData filterRight;
  /// Bootstrap icon for filter square fill.
  static const IconData filterSquareFill;
  /// Bootstrap icon for filter square.
  static const IconData filterSquare;
  /// Bootstrap icon for filter.
  static const IconData filter;
  /// Bootstrap icon for flag fill.
  static const IconData flagFill;
  /// Bootstrap icon for flag.
  static const IconData flag;
  /// Bootstrap icon for flower1.
  static const IconData flower1;
  /// Bootstrap icon for flower2.
  static const IconData flower2;
  /// Bootstrap icon for flower3.
  static const IconData flower3;
  /// Bootstrap icon for folder check.
  static const IconData folderCheck;
  /// Bootstrap icon for folder fill.
  static const IconData folderFill;
  /// Bootstrap icon for folder minus.
  static const IconData folderMinus;
  /// Bootstrap icon for folder plus.
  static const IconData folderPlus;
  /// Bootstrap icon for folder symlink fill.
  static const IconData folderSymlinkFill;
  /// Bootstrap icon for folder symlink.
  static const IconData folderSymlink;
  /// Bootstrap icon for folder x.
  static const IconData folderX;
  /// Bootstrap icon for folder.
  static const IconData folder;
  /// Bootstrap icon for folder2 open.
  static const IconData folder2Open;
  /// Bootstrap icon for folder2.
  static const IconData folder2;
  /// Bootstrap icon for fonts.
  static const IconData fonts;
  /// Bootstrap icon for forward fill.
  static const IconData forwardFill;
  /// Bootstrap icon for forward.
  static const IconData forward;
  /// Bootstrap icon for front.
  static const IconData front;
  /// Bootstrap icon for fullscreen exit.
  static const IconData fullscreenExit;
  /// Bootstrap icon for fullscreen.
  static const IconData fullscreen;
  /// Bootstrap icon for funnel fill.
  static const IconData funnelFill;
  /// Bootstrap icon for funnel.
  static const IconData funnel;
  /// Bootstrap icon for gear fill.
  static const IconData gearFill;
  /// Bootstrap icon for gear wide connected.
  static const IconData gearWideConnected;
  /// Bootstrap icon for gear wide.
  static const IconData gearWide;
  /// Bootstrap icon for gear.
  static const IconData gear;
  /// Bootstrap icon for gem.
  static const IconData gem;
  /// Bootstrap icon for geo alt fill.
  static const IconData geoAltFill;
  /// Bootstrap icon for geo alt.
  static const IconData geoAlt;
  /// Bootstrap icon for geo fill.
  static const IconData geoFill;
  /// Bootstrap icon for geo.
  static const IconData geo;
  /// Bootstrap icon for gift fill.
  static const IconData giftFill;
  /// Bootstrap icon for gift.
  static const IconData gift;
  /// Bootstrap icon for github.
  static const IconData github;
  /// Bootstrap icon for globe.
  static const IconData globe;
  /// Bootstrap icon for globe2.
  static const IconData globe2;
  /// Bootstrap icon for google.
  static const IconData google;
  /// Bootstrap icon for graph down.
  static const IconData graphDown;
  /// Bootstrap icon for graph up.
  static const IconData graphUp;
  /// Bootstrap icon for grid1x2 fill.
  static const IconData grid1x2Fill;
  /// Bootstrap icon for grid1x2.
  static const IconData grid1x2;
  /// Bootstrap icon for grid3x2 gap fill.
  static const IconData grid3x2GapFill;
  /// Bootstrap icon for grid3x2 gap.
  static const IconData grid3x2Gap;
  /// Bootstrap icon for grid3x2.
  static const IconData grid3x2;
  /// Bootstrap icon for grid3x3 gap fill.
  static const IconData grid3x3GapFill;
  /// Bootstrap icon for grid3x3 gap.
  static const IconData grid3x3Gap;
  /// Bootstrap icon for grid3x3.
  static const IconData grid3x3;
  /// Bootstrap icon for grid fill.
  static const IconData gridFill;
  /// Bootstrap icon for grid.
  static const IconData grid;
  /// Bootstrap icon for grip horizontal.
  static const IconData gripHorizontal;
  /// Bootstrap icon for grip vertical.
  static const IconData gripVertical;
  /// Bootstrap icon for hammer.
  static const IconData hammer;
  /// Bootstrap icon for hand index fill.
  static const IconData handIndexFill;
  /// Bootstrap icon for hand index thumb fill.
  static const IconData handIndexThumbFill;
  /// Bootstrap icon for hand index thumb.
  static const IconData handIndexThumb;
  /// Bootstrap icon for hand index.
  static const IconData handIndex;
  /// Bootstrap icon for hand thumbs down fill.
  static const IconData handThumbsDownFill;
  /// Bootstrap icon for hand thumbs down.
  static const IconData handThumbsDown;
  /// Bootstrap icon for hand thumbs up fill.
  static const IconData handThumbsUpFill;
  /// Bootstrap icon for hand thumbs up.
  static const IconData handThumbsUp;
  /// Bootstrap icon for handbag fill.
  static const IconData handbagFill;
  /// Bootstrap icon for handbag.
  static const IconData handbag;
  /// Bootstrap icon for hash.
  static const IconData hash;
  /// Bootstrap icon for hdd fill.
  static const IconData hddFill;
  /// Bootstrap icon for hdd network fill.
  static const IconData hddNetworkFill;
  /// Bootstrap icon for hdd network.
  static const IconData hddNetwork;
  /// Bootstrap icon for hdd rack fill.
  static const IconData hddRackFill;
  /// Bootstrap icon for hdd rack.
  static const IconData hddRack;
  /// Bootstrap icon for hdd stack fill.
  static const IconData hddStackFill;
  /// Bootstrap icon for hdd stack.
  static const IconData hddStack;
  /// Bootstrap icon for hdd.
  static const IconData hdd;
  /// Bootstrap icon for headphones.
  static const IconData headphones;
  /// Bootstrap icon for headset.
  static const IconData headset;
  /// Bootstrap icon for heart fill.
  static const IconData heartFill;
  /// Bootstrap icon for heart half.
  static const IconData heartHalf;
  /// Bootstrap icon for heart.
  static const IconData heart;
  /// Bootstrap icon for heptagon fill.
  static const IconData heptagonFill;
  /// Bootstrap icon for heptagon half.
  static const IconData heptagonHalf;
  /// Bootstrap icon for heptagon.
  static const IconData heptagon;
  /// Bootstrap icon for hexagon fill.
  static const IconData hexagonFill;
  /// Bootstrap icon for hexagon half.
  static const IconData hexagonHalf;
  /// Bootstrap icon for hexagon.
  static const IconData hexagon;
  /// Bootstrap icon for hourglass bottom.
  static const IconData hourglassBottom;
  /// Bootstrap icon for hourglass split.
  static const IconData hourglassSplit;
  /// Bootstrap icon for hourglass top.
  static const IconData hourglassTop;
  /// Bootstrap icon for hourglass.
  static const IconData hourglass;
  /// Bootstrap icon for house door fill.
  static const IconData houseDoorFill;
  /// Bootstrap icon for house door.
  static const IconData houseDoor;
  /// Bootstrap icon for house fill.
  static const IconData houseFill;
  /// Bootstrap icon for house.
  static const IconData house;
  /// Bootstrap icon for hr.
  static const IconData hr;
  /// Bootstrap icon for hurricane.
  static const IconData hurricane;
  /// Bootstrap icon for image alt.
  static const IconData imageAlt;
  /// Bootstrap icon for image fill.
  static const IconData imageFill;
  /// Bootstrap icon for image.
  static const IconData image;
  /// Bootstrap icon for images.
  static const IconData images;
  /// Bootstrap icon for inbox fill.
  static const IconData inboxFill;
  /// Bootstrap icon for inbox.
  static const IconData inbox;
  /// Bootstrap icon for inboxes fill.
  static const IconData inboxesFill;
  /// Bootstrap icon for inboxes.
  static const IconData inboxes;
  /// Bootstrap icon for info circle fill.
  static const IconData infoCircleFill;
  /// Bootstrap icon for info circle.
  static const IconData infoCircle;
  /// Bootstrap icon for info square fill.
  static const IconData infoSquareFill;
  /// Bootstrap icon for info square.
  static const IconData infoSquare;
  /// Bootstrap icon for info.
  static const IconData info;
  /// Bootstrap icon for input cursor text.
  static const IconData inputCursorText;
  /// Bootstrap icon for input cursor.
  static const IconData inputCursor;
  /// Bootstrap icon for instagram.
  static const IconData instagram;
  /// Bootstrap icon for intersect.
  static const IconData intersect;
  /// Bootstrap icon for journal album.
  static const IconData journalAlbum;
  /// Bootstrap icon for journal arrow down.
  static const IconData journalArrowDown;
  /// Bootstrap icon for journal arrow up.
  static const IconData journalArrowUp;
  /// Bootstrap icon for journal bookmark fill.
  static const IconData journalBookmarkFill;
  /// Bootstrap icon for journal bookmark.
  static const IconData journalBookmark;
  /// Bootstrap icon for journal check.
  static const IconData journalCheck;
  /// Bootstrap icon for journal code.
  static const IconData journalCode;
  /// Bootstrap icon for journal medical.
  static const IconData journalMedical;
  /// Bootstrap icon for journal minus.
  static const IconData journalMinus;
  /// Bootstrap icon for journal plus.
  static const IconData journalPlus;
  /// Bootstrap icon for journal richtext.
  static const IconData journalRichtext;
  /// Bootstrap icon for journal text.
  static const IconData journalText;
  /// Bootstrap icon for journal x.
  static const IconData journalX;
  /// Bootstrap icon for journal.
  static const IconData journal;
  /// Bootstrap icon for journals.
  static const IconData journals;
  /// Bootstrap icon for joystick.
  static const IconData joystick;
  /// Bootstrap icon for justify left.
  static const IconData justifyLeft;
  /// Bootstrap icon for justify right.
  static const IconData justifyRight;
  /// Bootstrap icon for justify.
  static const IconData justify;
  /// Bootstrap icon for kanban fill.
  static const IconData kanbanFill;
  /// Bootstrap icon for kanban.
  static const IconData kanban;
  /// Bootstrap icon for key fill.
  static const IconData keyFill;
  /// Bootstrap icon for key.
  static const IconData key;
  /// Bootstrap icon for keyboard fill.
  static const IconData keyboardFill;
  /// Bootstrap icon for keyboard.
  static const IconData keyboard;
  /// Bootstrap icon for ladder.
  static const IconData ladder;
  /// Bootstrap icon for lamp fill.
  static const IconData lampFill;
  /// Bootstrap icon for lamp.
  static const IconData lamp;
  /// Bootstrap icon for laptop fill.
  static const IconData laptopFill;
  /// Bootstrap icon for laptop.
  static const IconData laptop;
  /// Bootstrap icon for layer backward.
  static const IconData layerBackward;
  /// Bootstrap icon for layer forward.
  static const IconData layerForward;
  /// Bootstrap icon for layers fill.
  static const IconData layersFill;
  /// Bootstrap icon for layers half.
  static const IconData layersHalf;
  /// Bootstrap icon for layers.
  static const IconData layers;
  /// Bootstrap icon for layout sidebar inset reverse.
  static const IconData layoutSidebarInsetReverse;
  /// Bootstrap icon for layout sidebar inset.
  static const IconData layoutSidebarInset;
  /// Bootstrap icon for layout sidebar reverse.
  static const IconData layoutSidebarReverse;
  /// Bootstrap icon for layout sidebar.
  static const IconData layoutSidebar;
  /// Bootstrap icon for layout split.
  static const IconData layoutSplit;
  /// Bootstrap icon for layout text sidebar reverse.
  static const IconData layoutTextSidebarReverse;
  /// Bootstrap icon for layout text sidebar.
  static const IconData layoutTextSidebar;
  /// Bootstrap icon for layout text window reverse.
  static const IconData layoutTextWindowReverse;
  /// Bootstrap icon for layout text window.
  static const IconData layoutTextWindow;
  /// Bootstrap icon for layout three columns.
  static const IconData layoutThreeColumns;
  /// Bootstrap icon for layout wtf.
  static const IconData layoutWtf;
  /// Bootstrap icon for life preserver.
  static const IconData lifePreserver;
  /// Bootstrap icon for lightbulb fill.
  static const IconData lightbulbFill;
  /// Bootstrap icon for lightbulb off fill.
  static const IconData lightbulbOffFill;
  /// Bootstrap icon for lightbulb off.
  static const IconData lightbulbOff;
  /// Bootstrap icon for lightbulb.
  static const IconData lightbulb;
  /// Bootstrap icon for lightning charge fill.
  static const IconData lightningChargeFill;
  /// Bootstrap icon for lightning charge.
  static const IconData lightningCharge;
  /// Bootstrap icon for lightning fill.
  static const IconData lightningFill;
  /// Bootstrap icon for lightning.
  static const IconData lightning;
  /// Bootstrap icon for link45deg.
  static const IconData link45deg;
  /// Bootstrap icon for link.
  static const IconData link;
  /// Bootstrap icon for linkedin.
  static const IconData linkedin;
  /// Bootstrap icon for list check.
  static const IconData listCheck;
  /// Bootstrap icon for list nested.
  static const IconData listNested;
  /// Bootstrap icon for list ol.
  static const IconData listOl;
  /// Bootstrap icon for list stars.
  static const IconData listStars;
  /// Bootstrap icon for list task.
  static const IconData listTask;
  /// Bootstrap icon for list ul.
  static const IconData listUl;
  /// Bootstrap icon for list.
  static const IconData list;
  /// Bootstrap icon for lock fill.
  static const IconData lockFill;
  /// Bootstrap icon for lock.
  static const IconData lock;
  /// Bootstrap icon for mailbox.
  static const IconData mailbox;
  /// Bootstrap icon for mailbox2.
  static const IconData mailbox2;
  /// Bootstrap icon for map fill.
  static const IconData mapFill;
  /// Bootstrap icon for map.
  static const IconData map;
  /// Bootstrap icon for markdown fill.
  static const IconData markdownFill;
  /// Bootstrap icon for markdown.
  static const IconData markdown;
  /// Bootstrap icon for mask.
  static const IconData mask;
  /// Bootstrap icon for megaphone fill.
  static const IconData megaphoneFill;
  /// Bootstrap icon for megaphone.
  static const IconData megaphone;
  /// Bootstrap icon for menu app fill.
  static const IconData menuAppFill;
  /// Bootstrap icon for menu app.
  static const IconData menuApp;
  /// Bootstrap icon for menu button fill.
  static const IconData menuButtonFill;
  /// Bootstrap icon for menu button wide fill.
  static const IconData menuButtonWideFill;
  /// Bootstrap icon for menu button wide.
  static const IconData menuButtonWide;
  /// Bootstrap icon for menu button.
  static const IconData menuButton;
  /// Bootstrap icon for menu down.
  static const IconData menuDown;
  /// Bootstrap icon for menu up.
  static const IconData menuUp;
  /// Bootstrap icon for mic fill.
  static const IconData micFill;
  /// Bootstrap icon for mic mute fill.
  static const IconData micMuteFill;
  /// Bootstrap icon for mic mute.
  static const IconData micMute;
  /// Bootstrap icon for mic.
  static const IconData mic;
  /// Bootstrap icon for minecart loaded.
  static const IconData minecartLoaded;
  /// Bootstrap icon for minecart.
  static const IconData minecart;
  /// Bootstrap icon for moisture.
  static const IconData moisture;
  /// Bootstrap icon for moon fill.
  static const IconData moonFill;
  /// Bootstrap icon for moon stars fill.
  static const IconData moonStarsFill;
  /// Bootstrap icon for moon stars.
  static const IconData moonStars;
  /// Bootstrap icon for moon.
  static const IconData moon;
  /// Bootstrap icon for mouse fill.
  static const IconData mouseFill;
  /// Bootstrap icon for mouse.
  static const IconData mouse;
  /// Bootstrap icon for mouse2 fill.
  static const IconData mouse2Fill;
  /// Bootstrap icon for mouse2.
  static const IconData mouse2;
  /// Bootstrap icon for mouse3 fill.
  static const IconData mouse3Fill;
  /// Bootstrap icon for mouse3.
  static const IconData mouse3;
  /// Bootstrap icon for music note beamed.
  static const IconData musicNoteBeamed;
  /// Bootstrap icon for music note list.
  static const IconData musicNoteList;
  /// Bootstrap icon for music note.
  static const IconData musicNote;
  /// Bootstrap icon for music player fill.
  static const IconData musicPlayerFill;
  /// Bootstrap icon for music player.
  static const IconData musicPlayer;
  /// Bootstrap icon for newspaper.
  static const IconData newspaper;
  /// Bootstrap icon for node minus fill.
  static const IconData nodeMinusFill;
  /// Bootstrap icon for node minus.
  static const IconData nodeMinus;
  /// Bootstrap icon for node plus fill.
  static const IconData nodePlusFill;
  /// Bootstrap icon for node plus.
  static const IconData nodePlus;
  /// Bootstrap icon for nut fill.
  static const IconData nutFill;
  /// Bootstrap icon for nut.
  static const IconData nut;
  /// Bootstrap icon for octagon fill.
  static const IconData octagonFill;
  /// Bootstrap icon for octagon half.
  static const IconData octagonHalf;
  /// Bootstrap icon for octagon.
  static const IconData octagon;
  /// Bootstrap icon for option.
  static const IconData option;
  /// Bootstrap icon for outlet.
  static const IconData outlet;
  /// Bootstrap icon for paint bucket.
  static const IconData paintBucket;
  /// Bootstrap icon for palette fill.
  static const IconData paletteFill;
  /// Bootstrap icon for palette.
  static const IconData palette;
  /// Bootstrap icon for palette2.
  static const IconData palette2;
  /// Bootstrap icon for paperclip.
  static const IconData paperclip;
  /// Bootstrap icon for paragraph.
  static const IconData paragraph;
  /// Bootstrap icon for patch check fill.
  static const IconData patchCheckFill;
  /// Bootstrap icon for patch check.
  static const IconData patchCheck;
  /// Bootstrap icon for patch exclamation fill.
  static const IconData patchExclamationFill;
  /// Bootstrap icon for patch exclamation.
  static const IconData patchExclamation;
  /// Bootstrap icon for patch minus fill.
  static const IconData patchMinusFill;
  /// Bootstrap icon for patch minus.
  static const IconData patchMinus;
  /// Bootstrap icon for patch plus fill.
  static const IconData patchPlusFill;
  /// Bootstrap icon for patch plus.
  static const IconData patchPlus;
  /// Bootstrap icon for patch question fill.
  static const IconData patchQuestionFill;
  /// Bootstrap icon for patch question.
  static const IconData patchQuestion;
  /// Bootstrap icon for pause btn fill.
  static const IconData pauseBtnFill;
  /// Bootstrap icon for pause btn.
  static const IconData pauseBtn;
  /// Bootstrap icon for pause circle fill.
  static const IconData pauseCircleFill;
  /// Bootstrap icon for pause circle.
  static const IconData pauseCircle;
  /// Bootstrap icon for pause fill.
  static const IconData pauseFill;
  /// Bootstrap icon for pause.
  static const IconData pause;
  /// Bootstrap icon for peace fill.
  static const IconData peaceFill;
  /// Bootstrap icon for peace.
  static const IconData peace;
  /// Bootstrap icon for pen fill.
  static const IconData penFill;
  /// Bootstrap icon for pen.
  static const IconData pen;
  /// Bootstrap icon for pencil fill.
  static const IconData pencilFill;
  /// Bootstrap icon for pencil square.
  static const IconData pencilSquare;
  /// Bootstrap icon for pencil.
  static const IconData pencil;
  /// Bootstrap icon for pentagon fill.
  static const IconData pentagonFill;
  /// Bootstrap icon for pentagon half.
  static const IconData pentagonHalf;
  /// Bootstrap icon for pentagon.
  static const IconData pentagon;
  /// Bootstrap icon for people fill.
  static const IconData peopleFill;
  /// Bootstrap icon for people.
  static const IconData people;
  /// Bootstrap icon for percent.
  static const IconData percent;
  /// Bootstrap icon for person badge fill.
  static const IconData personBadgeFill;
  /// Bootstrap icon for person badge.
  static const IconData personBadge;
  /// Bootstrap icon for person bounding box.
  static const IconData personBoundingBox;
  /// Bootstrap icon for person check fill.
  static const IconData personCheckFill;
  /// Bootstrap icon for person check.
  static const IconData personCheck;
  /// Bootstrap icon for person circle.
  static const IconData personCircle;
  /// Bootstrap icon for person dash fill.
  static const IconData personDashFill;
  /// Bootstrap icon for person dash.
  static const IconData personDash;
  /// Bootstrap icon for person fill.
  static const IconData personFill;
  /// Bootstrap icon for person lines fill.
  static const IconData personLinesFill;
  /// Bootstrap icon for person plus fill.
  static const IconData personPlusFill;
  /// Bootstrap icon for person plus.
  static const IconData personPlus;
  /// Bootstrap icon for person square.
  static const IconData personSquare;
  /// Bootstrap icon for person x fill.
  static const IconData personXFill;
  /// Bootstrap icon for person x.
  static const IconData personX;
  /// Bootstrap icon for person.
  static const IconData person;
  /// Bootstrap icon for phone fill.
  static const IconData phoneFill;
  /// Bootstrap icon for phone landscape fill.
  static const IconData phoneLandscapeFill;
  /// Bootstrap icon for phone landscape.
  static const IconData phoneLandscape;
  /// Bootstrap icon for phone vibrate fill.
  static const IconData phoneVibrateFill;
  /// Bootstrap icon for phone vibrate.
  static const IconData phoneVibrate;
  /// Bootstrap icon for phone.
  static const IconData phone;
  /// Bootstrap icon for pie chart fill.
  static const IconData pieChartFill;
  /// Bootstrap icon for pie chart.
  static const IconData pieChart;
  /// Bootstrap icon for pin angle fill.
  static const IconData pinAngleFill;
  /// Bootstrap icon for pin angle.
  static const IconData pinAngle;
  /// Bootstrap icon for pin fill.
  static const IconData pinFill;
  /// Bootstrap icon for pin.
  static const IconData pin;
  /// Bootstrap icon for pip fill.
  static const IconData pipFill;
  /// Bootstrap icon for pip.
  static const IconData pip;
  /// Bootstrap icon for play btn fill.
  static const IconData playBtnFill;
  /// Bootstrap icon for play btn.
  static const IconData playBtn;
  /// Bootstrap icon for play circle fill.
  static const IconData playCircleFill;
  /// Bootstrap icon for play circle.
  static const IconData playCircle;
  /// Bootstrap icon for play fill.
  static const IconData playFill;
  /// Bootstrap icon for play.
  static const IconData play;
  /// Bootstrap icon for plug fill.
  static const IconData plugFill;
  /// Bootstrap icon for plug.
  static const IconData plug;
  /// Bootstrap icon for plus circle dotted.
  static const IconData plusCircleDotted;
  /// Bootstrap icon for plus circle fill.
  static const IconData plusCircleFill;
  /// Bootstrap icon for plus circle.
  static const IconData plusCircle;
  /// Bootstrap icon for plus square dotted.
  static const IconData plusSquareDotted;
  /// Bootstrap icon for plus square fill.
  static const IconData plusSquareFill;
  /// Bootstrap icon for plus square.
  static const IconData plusSquare;
  /// Bootstrap icon for plus.
  static const IconData plus;
  /// Bootstrap icon for power.
  static const IconData power;
  /// Bootstrap icon for printer fill.
  static const IconData printerFill;
  /// Bootstrap icon for printer.
  static const IconData printer;
  /// Bootstrap icon for puzzle fill.
  static const IconData puzzleFill;
  /// Bootstrap icon for puzzle.
  static const IconData puzzle;
  /// Bootstrap icon for question circle fill.
  static const IconData questionCircleFill;
  /// Bootstrap icon for question circle.
  static const IconData questionCircle;
  /// Bootstrap icon for question diamond fill.
  static const IconData questionDiamondFill;
  /// Bootstrap icon for question diamond.
  static const IconData questionDiamond;
  /// Bootstrap icon for question octagon fill.
  static const IconData questionOctagonFill;
  /// Bootstrap icon for question octagon.
  static const IconData questionOctagon;
  /// Bootstrap icon for question square fill.
  static const IconData questionSquareFill;
  /// Bootstrap icon for question square.
  static const IconData questionSquare;
  /// Bootstrap icon for question.
  static const IconData question;
  /// Bootstrap icon for rainbow.
  static const IconData rainbow;
  /// Bootstrap icon for receipt cutoff.
  static const IconData receiptCutoff;
  /// Bootstrap icon for receipt.
  static const IconData receipt;
  /// Bootstrap icon for reception0.
  static const IconData reception0;
  /// Bootstrap icon for reception1.
  static const IconData reception1;
  /// Bootstrap icon for reception2.
  static const IconData reception2;
  /// Bootstrap icon for reception3.
  static const IconData reception3;
  /// Bootstrap icon for reception4.
  static const IconData reception4;
  /// Bootstrap icon for record btn fill.
  static const IconData recordBtnFill;
  /// Bootstrap icon for record btn.
  static const IconData recordBtn;
  /// Bootstrap icon for record circle fill.
  static const IconData recordCircleFill;
  /// Bootstrap icon for record circle.
  static const IconData recordCircle;
  /// Bootstrap icon for record fill.
  static const IconData recordFill;
  /// Bootstrap icon for record.
  static const IconData record;
  /// Bootstrap icon for record2 fill.
  static const IconData record2Fill;
  /// Bootstrap icon for record2.
  static const IconData record2;
  /// Bootstrap icon for reply all fill.
  static const IconData replyAllFill;
  /// Bootstrap icon for reply all.
  static const IconData replyAll;
  /// Bootstrap icon for reply fill.
  static const IconData replyFill;
  /// Bootstrap icon for reply.
  static const IconData reply;
  /// Bootstrap icon for rss fill.
  static const IconData rssFill;
  /// Bootstrap icon for rss.
  static const IconData rss;
  /// Bootstrap icon for rulers.
  static const IconData rulers;
  /// Bootstrap icon for save fill.
  static const IconData saveFill;
  /// Bootstrap icon for save.
  static const IconData save;
  /// Bootstrap icon for save2 fill.
  static const IconData save2Fill;
  /// Bootstrap icon for save2.
  static const IconData save2;
  /// Bootstrap icon for scissors.
  static const IconData scissors;
  /// Bootstrap icon for screwdriver.
  static const IconData screwdriver;
  /// Bootstrap icon for search.
  static const IconData search;
  /// Bootstrap icon for segmented nav.
  static const IconData segmentedNav;
  /// Bootstrap icon for server.
  static const IconData server;
  /// Bootstrap icon for share fill.
  static const IconData shareFill;
  /// Bootstrap icon for share.
  static const IconData share;
  /// Bootstrap icon for shield check.
  static const IconData shieldCheck;
  /// Bootstrap icon for shield exclamation.
  static const IconData shieldExclamation;
  /// Bootstrap icon for shield fill check.
  static const IconData shieldFillCheck;
  /// Bootstrap icon for shield fill exclamation.
  static const IconData shieldFillExclamation;
  /// Bootstrap icon for shield fill minus.
  static const IconData shieldFillMinus;
  /// Bootstrap icon for shield fill plus.
  static const IconData shieldFillPlus;
  /// Bootstrap icon for shield fill x.
  static const IconData shieldFillX;
  /// Bootstrap icon for shield fill.
  static const IconData shieldFill;
  /// Bootstrap icon for shield lock fill.
  static const IconData shieldLockFill;
  /// Bootstrap icon for shield lock.
  static const IconData shieldLock;
  /// Bootstrap icon for shield minus.
  static const IconData shieldMinus;
  /// Bootstrap icon for shield plus.
  static const IconData shieldPlus;
  /// Bootstrap icon for shield shaded.
  static const IconData shieldShaded;
  /// Bootstrap icon for shield slash fill.
  static const IconData shieldSlashFill;
  /// Bootstrap icon for shield slash.
  static const IconData shieldSlash;
  /// Bootstrap icon for shield x.
  static const IconData shieldX;
  /// Bootstrap icon for shield.
  static const IconData shield;
  /// Bootstrap icon for shift fill.
  static const IconData shiftFill;
  /// Bootstrap icon for shift.
  static const IconData shift;
  /// Bootstrap icon for shop window.
  static const IconData shopWindow;
  /// Bootstrap icon for shop.
  static const IconData shop;
  /// Bootstrap icon for shuffle.
  static const IconData shuffle;
  /// Bootstrap icon for signpost2 fill.
  static const IconData signpost2Fill;
  /// Bootstrap icon for signpost2.
  static const IconData signpost2;
  /// Bootstrap icon for signpost fill.
  static const IconData signpostFill;
  /// Bootstrap icon for signpost split fill.
  static const IconData signpostSplitFill;
  /// Bootstrap icon for signpost split.
  static const IconData signpostSplit;
  /// Bootstrap icon for signpost.
  static const IconData signpost;
  /// Bootstrap icon for sim fill.
  static const IconData simFill;
  /// Bootstrap icon for sim.
  static const IconData sim;
  /// Bootstrap icon for skip backward btn fill.
  static const IconData skipBackwardBtnFill;
  /// Bootstrap icon for skip backward btn.
  static const IconData skipBackwardBtn;
  /// Bootstrap icon for skip backward circle fill.
  static const IconData skipBackwardCircleFill;
  /// Bootstrap icon for skip backward circle.
  static const IconData skipBackwardCircle;
  /// Bootstrap icon for skip backward fill.
  static const IconData skipBackwardFill;
  /// Bootstrap icon for skip backward.
  static const IconData skipBackward;
  /// Bootstrap icon for skip end btn fill.
  static const IconData skipEndBtnFill;
  /// Bootstrap icon for skip end btn.
  static const IconData skipEndBtn;
  /// Bootstrap icon for skip end circle fill.
  static const IconData skipEndCircleFill;
  /// Bootstrap icon for skip end circle.
  static const IconData skipEndCircle;
  /// Bootstrap icon for skip end fill.
  static const IconData skipEndFill;
  /// Bootstrap icon for skip end.
  static const IconData skipEnd;
  /// Bootstrap icon for skip forward btn fill.
  static const IconData skipForwardBtnFill;
  /// Bootstrap icon for skip forward btn.
  static const IconData skipForwardBtn;
  /// Bootstrap icon for skip forward circle fill.
  static const IconData skipForwardCircleFill;
  /// Bootstrap icon for skip forward circle.
  static const IconData skipForwardCircle;
  /// Bootstrap icon for skip forward fill.
  static const IconData skipForwardFill;
  /// Bootstrap icon for skip forward.
  static const IconData skipForward;
  /// Bootstrap icon for skip start btn fill.
  static const IconData skipStartBtnFill;
  /// Bootstrap icon for skip start btn.
  static const IconData skipStartBtn;
  /// Bootstrap icon for skip start circle fill.
  static const IconData skipStartCircleFill;
  /// Bootstrap icon for skip start circle.
  static const IconData skipStartCircle;
  /// Bootstrap icon for skip start fill.
  static const IconData skipStartFill;
  /// Bootstrap icon for skip start.
  static const IconData skipStart;
  /// Bootstrap icon for slack.
  static const IconData slack;
  /// Bootstrap icon for slash circle fill.
  static const IconData slashCircleFill;
  /// Bootstrap icon for slash circle.
  static const IconData slashCircle;
  /// Bootstrap icon for slash square fill.
  static const IconData slashSquareFill;
  /// Bootstrap icon for slash square.
  static const IconData slashSquare;
  /// Bootstrap icon for slash.
  static const IconData slash;
  /// Bootstrap icon for sliders.
  static const IconData sliders;
  /// Bootstrap icon for smartwatch.
  static const IconData smartwatch;
  /// Bootstrap icon for snow.
  static const IconData snow;
  /// Bootstrap icon for snow2.
  static const IconData snow2;
  /// Bootstrap icon for snow3.
  static const IconData snow3;
  /// Bootstrap icon for sort alpha down alt.
  static const IconData sortAlphaDownAlt;
  /// Bootstrap icon for sort alpha down.
  static const IconData sortAlphaDown;
  /// Bootstrap icon for sort alpha up alt.
  static const IconData sortAlphaUpAlt;
  /// Bootstrap icon for sort alpha up.
  static const IconData sortAlphaUp;
  /// Bootstrap icon for sort down alt.
  static const IconData sortDownAlt;
  /// Bootstrap icon for sort down.
  static const IconData sortDown;
  /// Bootstrap icon for sort numeric down alt.
  static const IconData sortNumericDownAlt;
  /// Bootstrap icon for sort numeric down.
  static const IconData sortNumericDown;
  /// Bootstrap icon for sort numeric up alt.
  static const IconData sortNumericUpAlt;
  /// Bootstrap icon for sort numeric up.
  static const IconData sortNumericUp;
  /// Bootstrap icon for sort up alt.
  static const IconData sortUpAlt;
  /// Bootstrap icon for sort up.
  static const IconData sortUp;
  /// Bootstrap icon for soundwave.
  static const IconData soundwave;
  /// Bootstrap icon for speaker fill.
  static const IconData speakerFill;
  /// Bootstrap icon for speaker.
  static const IconData speaker;
  /// Bootstrap icon for speedometer.
  static const IconData speedometer;
  /// Bootstrap icon for speedometer2.
  static const IconData speedometer2;
  /// Bootstrap icon for spellcheck.
  static const IconData spellcheck;
  /// Bootstrap icon for square fill.
  static const IconData squareFill;
  /// Bootstrap icon for square half.
  static const IconData squareHalf;
  /// Bootstrap icon for square.
  static const IconData square;
  /// Bootstrap icon for stack.
  static const IconData stack;
  /// Bootstrap icon for star fill.
  static const IconData starFill;
  /// Bootstrap icon for star half.
  static const IconData starHalf;
  /// Bootstrap icon for star.
  static const IconData star;
  /// Bootstrap icon for stars.
  static const IconData stars;
  /// Bootstrap icon for stickies fill.
  static const IconData stickiesFill;
  /// Bootstrap icon for stickies.
  static const IconData stickies;
  /// Bootstrap icon for sticky fill.
  static const IconData stickyFill;
  /// Bootstrap icon for sticky.
  static const IconData sticky;
  /// Bootstrap icon for stop btn fill.
  static const IconData stopBtnFill;
  /// Bootstrap icon for stop btn.
  static const IconData stopBtn;
  /// Bootstrap icon for stop circle fill.
  static const IconData stopCircleFill;
  /// Bootstrap icon for stop circle.
  static const IconData stopCircle;
  /// Bootstrap icon for stop fill.
  static const IconData stopFill;
  /// Bootstrap icon for stop.
  static const IconData stop;
  /// Bootstrap icon for stoplights fill.
  static const IconData stoplightsFill;
  /// Bootstrap icon for stoplights.
  static const IconData stoplights;
  /// Bootstrap icon for stopwatch fill.
  static const IconData stopwatchFill;
  /// Bootstrap icon for stopwatch.
  static const IconData stopwatch;
  /// Bootstrap icon for subtract.
  static const IconData subtract;
  /// Bootstrap icon for suit club fill.
  static const IconData suitClubFill;
  /// Bootstrap icon for suit club.
  static const IconData suitClub;
  /// Bootstrap icon for suit diamond fill.
  static const IconData suitDiamondFill;
  /// Bootstrap icon for suit diamond.
  static const IconData suitDiamond;
  /// Bootstrap icon for suit heart fill.
  static const IconData suitHeartFill;
  /// Bootstrap icon for suit heart.
  static const IconData suitHeart;
  /// Bootstrap icon for suit spade fill.
  static const IconData suitSpadeFill;
  /// Bootstrap icon for suit spade.
  static const IconData suitSpade;
  /// Bootstrap icon for sun fill.
  static const IconData sunFill;
  /// Bootstrap icon for sun.
  static const IconData sun;
  /// Bootstrap icon for sunglasses.
  static const IconData sunglasses;
  /// Bootstrap icon for sunrise fill.
  static const IconData sunriseFill;
  /// Bootstrap icon for sunrise.
  static const IconData sunrise;
  /// Bootstrap icon for sunset fill.
  static const IconData sunsetFill;
  /// Bootstrap icon for sunset.
  static const IconData sunset;
  /// Bootstrap icon for symmetry horizontal.
  static const IconData symmetryHorizontal;
  /// Bootstrap icon for symmetry vertical.
  static const IconData symmetryVertical;
  /// Bootstrap icon for table.
  static const IconData table;
  /// Bootstrap icon for tablet fill.
  static const IconData tabletFill;
  /// Bootstrap icon for tablet landscape fill.
  static const IconData tabletLandscapeFill;
  /// Bootstrap icon for tablet landscape.
  static const IconData tabletLandscape;
  /// Bootstrap icon for tablet.
  static const IconData tablet;
  /// Bootstrap icon for tag fill.
  static const IconData tagFill;
  /// Bootstrap icon for tag.
  static const IconData tag;
  /// Bootstrap icon for tags fill.
  static const IconData tagsFill;
  /// Bootstrap icon for tags.
  static const IconData tags;
  /// Bootstrap icon for telegram.
  static const IconData telegram;
  /// Bootstrap icon for telephone fill.
  static const IconData telephoneFill;
  /// Bootstrap icon for telephone forward fill.
  static const IconData telephoneForwardFill;
  /// Bootstrap icon for telephone forward.
  static const IconData telephoneForward;
  /// Bootstrap icon for telephone inbound fill.
  static const IconData telephoneInboundFill;
  /// Bootstrap icon for telephone inbound.
  static const IconData telephoneInbound;
  /// Bootstrap icon for telephone minus fill.
  static const IconData telephoneMinusFill;
  /// Bootstrap icon for telephone minus.
  static const IconData telephoneMinus;
  /// Bootstrap icon for telephone outbound fill.
  static const IconData telephoneOutboundFill;
  /// Bootstrap icon for telephone outbound.
  static const IconData telephoneOutbound;
  /// Bootstrap icon for telephone plus fill.
  static const IconData telephonePlusFill;
  /// Bootstrap icon for telephone plus.
  static const IconData telephonePlus;
  /// Bootstrap icon for telephone x fill.
  static const IconData telephoneXFill;
  /// Bootstrap icon for telephone x.
  static const IconData telephoneX;
  /// Bootstrap icon for telephone.
  static const IconData telephone;
  /// Bootstrap icon for terminal fill.
  static const IconData terminalFill;
  /// Bootstrap icon for terminal.
  static const IconData terminal;
  /// Bootstrap icon for text center.
  static const IconData textCenter;
  /// Bootstrap icon for text indent left.
  static const IconData textIndentLeft;
  /// Bootstrap icon for text indent right.
  static const IconData textIndentRight;
  /// Bootstrap icon for text left.
  static const IconData textLeft;
  /// Bootstrap icon for text paragraph.
  static const IconData textParagraph;
  /// Bootstrap icon for text right.
  static const IconData textRight;
  /// Bootstrap icon for textarea resize.
  static const IconData textareaResize;
  /// Bootstrap icon for textarea t.
  static const IconData textareaT;
  /// Bootstrap icon for textarea.
  static const IconData textarea;
  /// Bootstrap icon for thermometer half.
  static const IconData thermometerHalf;
  /// Bootstrap icon for thermometer high.
  static const IconData thermometerHigh;
  /// Bootstrap icon for thermometer low.
  static const IconData thermometerLow;
  /// Bootstrap icon for thermometer snow.
  static const IconData thermometerSnow;
  /// Bootstrap icon for thermometer sun.
  static const IconData thermometerSun;
  /// Bootstrap icon for thermometer.
  static const IconData thermometer;
  /// Bootstrap icon for three dots vertical.
  static const IconData threeDotsVertical;
  /// Bootstrap icon for three dots.
  static const IconData threeDots;
  /// Bootstrap icon for toggle off.
  static const IconData toggleOff;
  /// Bootstrap icon for toggle on.
  static const IconData toggleOn;
  /// Bootstrap icon for toggle2 off.
  static const IconData toggle2Off;
  /// Bootstrap icon for toggle2 on.
  static const IconData toggle2On;
  /// Bootstrap icon for toggles.
  static const IconData toggles;
  /// Bootstrap icon for toggles2.
  static const IconData toggles2;
  /// Bootstrap icon for tools.
  static const IconData tools;
  /// Bootstrap icon for tornado.
  static const IconData tornado;
  /// Bootstrap icon for trash fill.
  static const IconData trashFill;
  /// Bootstrap icon for trash.
  static const IconData trash;
  /// Bootstrap icon for trash2 fill.
  static const IconData trash2Fill;
  /// Bootstrap icon for trash2.
  static const IconData trash2;
  /// Bootstrap icon for tree fill.
  static const IconData treeFill;
  /// Bootstrap icon for tree.
  static const IconData tree;
  /// Bootstrap icon for triangle fill.
  static const IconData triangleFill;
  /// Bootstrap icon for triangle half.
  static const IconData triangleHalf;
  /// Bootstrap icon for triangle.
  static const IconData triangle;
  /// Bootstrap icon for trophy fill.
  static const IconData trophyFill;
  /// Bootstrap icon for trophy.
  static const IconData trophy;
  /// Bootstrap icon for tropical storm.
  static const IconData tropicalStorm;
  /// Bootstrap icon for truck flatbed.
  static const IconData truckFlatbed;
  /// Bootstrap icon for truck.
  static const IconData truck;
  /// Bootstrap icon for tsunami.
  static const IconData tsunami;
  /// Bootstrap icon for tv fill.
  static const IconData tvFill;
  /// Bootstrap icon for tv.
  static const IconData tv;
  /// Bootstrap icon for twitch.
  static const IconData twitch;
  /// Bootstrap icon for twitter.
  static const IconData twitter;
  /// Bootstrap icon for type bold.
  static const IconData typeBold;
  /// Bootstrap icon for type h1.
  static const IconData typeH1;
  /// Bootstrap icon for type h2.
  static const IconData typeH2;
  /// Bootstrap icon for type h3.
  static const IconData typeH3;
  /// Bootstrap icon for type italic.
  static const IconData typeItalic;
  /// Bootstrap icon for type strikethrough.
  static const IconData typeStrikethrough;
  /// Bootstrap icon for type underline.
  static const IconData typeUnderline;
  /// Bootstrap icon for type.
  static const IconData type;
  /// Bootstrap icon for ui checks grid.
  static const IconData uiChecksGrid;
  /// Bootstrap icon for ui checks.
  static const IconData uiChecks;
  /// Bootstrap icon for ui radios grid.
  static const IconData uiRadiosGrid;
  /// Bootstrap icon for ui radios.
  static const IconData uiRadios;
  /// Bootstrap icon for umbrella fill.
  static const IconData umbrellaFill;
  /// Bootstrap icon for umbrella.
  static const IconData umbrella;
  /// Bootstrap icon for union.
  static const IconData union;
  /// Bootstrap icon for unlock fill.
  static const IconData unlockFill;
  /// Bootstrap icon for unlock.
  static const IconData unlock;
  /// Bootstrap icon for upc scan.
  static const IconData upcScan;
  /// Bootstrap icon for upc.
  static const IconData upc;
  /// Bootstrap icon for upload.
  static const IconData upload;
  /// Bootstrap icon for vector pen.
  static const IconData vectorPen;
  /// Bootstrap icon for view list.
  static const IconData viewList;
  /// Bootstrap icon for view stacked.
  static const IconData viewStacked;
  /// Bootstrap icon for vinyl fill.
  static const IconData vinylFill;
  /// Bootstrap icon for vinyl.
  static const IconData vinyl;
  /// Bootstrap icon for voicemail.
  static const IconData voicemail;
  /// Bootstrap icon for volume down fill.
  static const IconData volumeDownFill;
  /// Bootstrap icon for volume down.
  static const IconData volumeDown;
  /// Bootstrap icon for volume mute fill.
  static const IconData volumeMuteFill;
  /// Bootstrap icon for volume mute.
  static const IconData volumeMute;
  /// Bootstrap icon for volume off fill.
  static const IconData volumeOffFill;
  /// Bootstrap icon for volume off.
  static const IconData volumeOff;
  /// Bootstrap icon for volume up fill.
  static const IconData volumeUpFill;
  /// Bootstrap icon for volume up.
  static const IconData volumeUp;
  /// Bootstrap icon for vr.
  static const IconData vr;
  /// Bootstrap icon for wallet fill.
  static const IconData walletFill;
  /// Bootstrap icon for wallet.
  static const IconData wallet;
  /// Bootstrap icon for wallet2.
  static const IconData wallet2;
  /// Bootstrap icon for watch.
  static const IconData watch;
  /// Bootstrap icon for water.
  static const IconData water;
  /// Bootstrap icon for whatsapp.
  static const IconData whatsapp;
  /// Bootstrap icon for wifi1.
  static const IconData wifi1;
  /// Bootstrap icon for wifi2.
  static const IconData wifi2;
  /// Bootstrap icon for wifi off.
  static const IconData wifiOff;
  /// Bootstrap icon for wifi.
  static const IconData wifi;
  /// Bootstrap icon for wind.
  static const IconData wind;
  /// Bootstrap icon for window dock.
  static const IconData windowDock;
  /// Bootstrap icon for window sidebar.
  static const IconData windowSidebar;
  /// Bootstrap icon for window.
  static const IconData window;
  /// Bootstrap icon for wrench.
  static const IconData wrench;
  /// Bootstrap icon for x circle fill.
  static const IconData xCircleFill;
  /// Bootstrap icon for x circle.
  static const IconData xCircle;
  /// Bootstrap icon for x diamond fill.
  static const IconData xDiamondFill;
  /// Bootstrap icon for x diamond.
  static const IconData xDiamond;
  /// Bootstrap icon for x octagon fill.
  static const IconData xOctagonFill;
  /// Bootstrap icon for x octagon.
  static const IconData xOctagon;
  /// Bootstrap icon for x square fill.
  static const IconData xSquareFill;
  /// Bootstrap icon for x square.
  static const IconData xSquare;
  /// Bootstrap icon for x.
  static const IconData x;
  /// Bootstrap icon for youtube.
  static const IconData youtube;
  /// Bootstrap icon for zoom in.
  static const IconData zoomIn;
  /// Bootstrap icon for zoom out.
  static const IconData zoomOut;
  /// Bootstrap icon for bank.
  static const IconData bank;
  /// Bootstrap icon for bank2.
  static const IconData bank2;
  /// Bootstrap icon for bell slash fill.
  static const IconData bellSlashFill;
  /// Bootstrap icon for bell slash.
  static const IconData bellSlash;
  /// Bootstrap icon for cash coin.
  static const IconData cashCoin;
  /// Bootstrap icon for check lg.
  static const IconData checkLg;
  /// Bootstrap icon for coin.
  static const IconData coin;
  /// Bootstrap icon for currency bitcoin.
  static const IconData currencyBitcoin;
  /// Bootstrap icon for currency dollar.
  static const IconData currencyDollar;
  /// Bootstrap icon for currency euro.
  static const IconData currencyEuro;
  /// Bootstrap icon for currency exchange.
  static const IconData currencyExchange;
  /// Bootstrap icon for currency pound.
  static const IconData currencyPound;
  /// Bootstrap icon for currency yen.
  static const IconData currencyYen;
  /// Bootstrap icon for dash lg.
  static const IconData dashLg;
  /// Bootstrap icon for exclamation lg.
  static const IconData exclamationLg;
  /// Bootstrap icon for file earmark pdf fill.
  static const IconData fileEarmarkPdfFill;
  /// Bootstrap icon for file earmark pdf.
  static const IconData fileEarmarkPdf;
  /// Bootstrap icon for file pdf fill.
  static const IconData filePdfFill;
  /// Bootstrap icon for file pdf.
  static const IconData filePdf;
  /// Bootstrap icon for gender ambiguous.
  static const IconData genderAmbiguous;
  /// Bootstrap icon for gender female.
  static const IconData genderFemale;
  /// Bootstrap icon for gender male.
  static const IconData genderMale;
  /// Bootstrap icon for gender trans.
  static const IconData genderTrans;
  /// Bootstrap icon for headset vr.
  static const IconData headsetVr;
  /// Bootstrap icon for info lg.
  static const IconData infoLg;
  /// Bootstrap icon for mastodon.
  static const IconData mastodon;
  /// Bootstrap icon for messenger.
  static const IconData messenger;
  /// Bootstrap icon for piggy bank fill.
  static const IconData piggyBankFill;
  /// Bootstrap icon for piggy bank.
  static const IconData piggyBank;
  /// Bootstrap icon for pin map fill.
  static const IconData pinMapFill;
  /// Bootstrap icon for pin map.
  static const IconData pinMap;
  /// Bootstrap icon for plus lg.
  static const IconData plusLg;
  /// Bootstrap icon for question lg.
  static const IconData questionLg;
  /// Bootstrap icon for recycle.
  static const IconData recycle;
  /// Bootstrap icon for reddit.
  static const IconData reddit;
  /// Bootstrap icon for safe fill.
  static const IconData safeFill;
  /// Bootstrap icon for safe2 fill.
  static const IconData safe2Fill;
  /// Bootstrap icon for safe2.
  static const IconData safe2;
  /// Bootstrap icon for sd card fill.
  static const IconData sdCardFill;
  /// Bootstrap icon for sd card.
  static const IconData sdCard;
  /// Bootstrap icon for skype.
  static const IconData skype;
  /// Bootstrap icon for slash lg.
  static const IconData slashLg;
  /// Bootstrap icon for translate.
  static const IconData translate;
  /// Bootstrap icon for x lg.
  static const IconData xLg;
  /// Bootstrap icon for safe.
  static const IconData safe;
  /// Bootstrap icon for apple.
  static const IconData apple;
  /// Bootstrap icon for microsoft.
  static const IconData microsoft;
  /// Bootstrap icon for windows.
  static const IconData windows;
  /// Bootstrap icon for behance.
  static const IconData behance;
  /// Bootstrap icon for dribbble.
  static const IconData dribbble;
  /// Bootstrap icon for line.
  static const IconData line;
  /// Bootstrap icon for medium.
  static const IconData medium;
  /// Bootstrap icon for paypal.
  static const IconData paypal;
  /// Bootstrap icon for pinterest.
  static const IconData pinterest;
  /// Bootstrap icon for signal.
  static const IconData signal;
  /// Bootstrap icon for snapchat.
  static const IconData snapchat;
  /// Bootstrap icon for spotify.
  static const IconData spotify;
  /// Bootstrap icon for stack overflow.
  static const IconData stackOverflow;
  /// Bootstrap icon for strava.
  static const IconData strava;
  /// Bootstrap icon for wordpress.
  static const IconData wordpress;
  /// Bootstrap icon for vimeo.
  static const IconData vimeo;
  /// Bootstrap icon for activity.
  static const IconData activity;
  /// Bootstrap icon for easel2 fill.
  static const IconData easel2Fill;
  /// Bootstrap icon for easel2.
  static const IconData easel2;
  /// Bootstrap icon for easel3 fill.
  static const IconData easel3Fill;
  /// Bootstrap icon for easel3.
  static const IconData easel3;
  /// Bootstrap icon for fan.
  static const IconData fan;
  /// Bootstrap icon for fingerprint.
  static const IconData fingerprint;
  /// Bootstrap icon for graph down arrow.
  static const IconData graphDownArrow;
  /// Bootstrap icon for graph up arrow.
  static const IconData graphUpArrow;
  /// Bootstrap icon for hypnotize.
  static const IconData hypnotize;
  /// Bootstrap icon for magic.
  static const IconData magic;
  /// Bootstrap icon for person rolodex.
  static const IconData personRolodex;
  /// Bootstrap icon for person video.
  static const IconData personVideo;
  /// Bootstrap icon for person video2.
  static const IconData personVideo2;
  /// Bootstrap icon for person video3.
  static const IconData personVideo3;
  /// Bootstrap icon for person workspace.
  static const IconData personWorkspace;
  /// Bootstrap icon for radioactive.
  static const IconData radioactive;
  /// Bootstrap icon for webcam fill.
  static const IconData webcamFill;
  /// Bootstrap icon for webcam.
  static const IconData webcam;
  /// Bootstrap icon for yin yang.
  static const IconData yinYang;
  /// Bootstrap icon for bandaid fill.
  static const IconData bandaidFill;
  /// Bootstrap icon for bandaid.
  static const IconData bandaid;
  /// Bootstrap icon for bluetooth.
  static const IconData bluetooth;
  /// Bootstrap icon for body text.
  static const IconData bodyText;
  /// Bootstrap icon for boombox.
  static const IconData boombox;
  /// Bootstrap icon for boxes.
  static const IconData boxes;
  /// Bootstrap icon for dpad fill.
  static const IconData dpadFill;
  /// Bootstrap icon for dpad.
  static const IconData dpad;
  /// Bootstrap icon for ear fill.
  static const IconData earFill;
  /// Bootstrap icon for ear.
  static const IconData ear;
  /// Bootstrap icon for envelope check fill.
  static const IconData envelopeCheckFill;
  /// Bootstrap icon for envelope check.
  static const IconData envelopeCheck;
  /// Bootstrap icon for envelope dash fill.
  static const IconData envelopeDashFill;
  /// Bootstrap icon for envelope dash.
  static const IconData envelopeDash;
  /// Bootstrap icon for envelope exclamation fill.
  static const IconData envelopeExclamationFill;
  /// Bootstrap icon for envelope exclamation.
  static const IconData envelopeExclamation;
  /// Bootstrap icon for envelope plus fill.
  static const IconData envelopePlusFill;
  /// Bootstrap icon for envelope plus.
  static const IconData envelopePlus;
  /// Bootstrap icon for envelope slash fill.
  static const IconData envelopeSlashFill;
  /// Bootstrap icon for envelope slash.
  static const IconData envelopeSlash;
  /// Bootstrap icon for envelope x fill.
  static const IconData envelopeXFill;
  /// Bootstrap icon for envelope x.
  static const IconData envelopeX;
  /// Bootstrap icon for explicit fill.
  static const IconData explicitFill;
  /// Bootstrap icon for explicit.
  static const IconData explicit;
  /// Bootstrap icon for git.
  static const IconData git;
  /// Bootstrap icon for infinity.
  static const IconData infinity;
  /// Bootstrap icon for list columns reverse.
  static const IconData listColumnsReverse;
  /// Bootstrap icon for list columns.
  static const IconData listColumns;
  /// Bootstrap icon for meta.
  static const IconData meta;
  /// Bootstrap icon for nintendo switch.
  static const IconData nintendoSwitch;
  /// Bootstrap icon for pc display horizontal.
  static const IconData pcDisplayHorizontal;
  /// Bootstrap icon for pc display.
  static const IconData pcDisplay;
  /// Bootstrap icon for pc horizontal.
  static const IconData pcHorizontal;
  /// Bootstrap icon for pc.
  static const IconData pc;
  /// Bootstrap icon for playstation.
  static const IconData playstation;
  /// Bootstrap icon for plus slash minus.
  static const IconData plusSlashMinus;
  /// Bootstrap icon for projector fill.
  static const IconData projectorFill;
  /// Bootstrap icon for projector.
  static const IconData projector;
  /// Bootstrap icon for qr code scan.
  static const IconData qrCodeScan;
  /// Bootstrap icon for qr code.
  static const IconData qrCode;
  /// Bootstrap icon for quora.
  static const IconData quora;
  /// Bootstrap icon for quote.
  static const IconData quote;
  /// Bootstrap icon for robot.
  static const IconData robot;
  /// Bootstrap icon for send check fill.
  static const IconData sendCheckFill;
  /// Bootstrap icon for send check.
  static const IconData sendCheck;
  /// Bootstrap icon for send dash fill.
  static const IconData sendDashFill;
  /// Bootstrap icon for send dash.
  static const IconData sendDash;
  /// Bootstrap icon for send exclamation fill.
  static const IconData sendExclamationFill;
  /// Bootstrap icon for send exclamation.
  static const IconData sendExclamation;
  /// Bootstrap icon for send fill.
  static const IconData sendFill;
  /// Bootstrap icon for send plus fill.
  static const IconData sendPlusFill;
  /// Bootstrap icon for send plus.
  static const IconData sendPlus;
  /// Bootstrap icon for send slash fill.
  static const IconData sendSlashFill;
  /// Bootstrap icon for send slash.
  static const IconData sendSlash;
  /// Bootstrap icon for send x fill.
  static const IconData sendXFill;
  /// Bootstrap icon for send x.
  static const IconData sendX;
  /// Bootstrap icon for send.
  static const IconData send;
  /// Bootstrap icon for steam.
  static const IconData steam;
  /// Bootstrap icon for terminal dash.
  static const IconData terminalDash;
  /// Bootstrap icon for terminal plus.
  static const IconData terminalPlus;
  /// Bootstrap icon for terminal split.
  static const IconData terminalSplit;
  /// Bootstrap icon for ticket detailed fill.
  static const IconData ticketDetailedFill;
  /// Bootstrap icon for ticket detailed.
  static const IconData ticketDetailed;
  /// Bootstrap icon for ticket fill.
  static const IconData ticketFill;
  /// Bootstrap icon for ticket perforated fill.
  static const IconData ticketPerforatedFill;
  /// Bootstrap icon for ticket perforated.
  static const IconData ticketPerforated;
  /// Bootstrap icon for ticket.
  static const IconData ticket;
  /// Bootstrap icon for tiktok.
  static const IconData tiktok;
  /// Bootstrap icon for window dash.
  static const IconData windowDash;
  /// Bootstrap icon for window desktop.
  static const IconData windowDesktop;
  /// Bootstrap icon for window fullscreen.
  static const IconData windowFullscreen;
  /// Bootstrap icon for window plus.
  static const IconData windowPlus;
  /// Bootstrap icon for window split.
  static const IconData windowSplit;
  /// Bootstrap icon for window stack.
  static const IconData windowStack;
  /// Bootstrap icon for window x.
  static const IconData windowX;
  /// Bootstrap icon for xbox.
  static const IconData xbox;
  /// Bootstrap icon for ethernet.
  static const IconData ethernet;
  /// Bootstrap icon for hdmi fill.
  static const IconData hdmiFill;
  /// Bootstrap icon for hdmi.
  static const IconData hdmi;
  /// Bootstrap icon for usb c fill.
  static const IconData usbCFill;
  /// Bootstrap icon for usb c.
  static const IconData usbC;
  /// Bootstrap icon for usb fill.
  static const IconData usbFill;
  /// Bootstrap icon for usb plug fill.
  static const IconData usbPlugFill;
  /// Bootstrap icon for usb plug.
  static const IconData usbPlug;
  /// Bootstrap icon for usb symbol.
  static const IconData usbSymbol;
  /// Bootstrap icon for usb.
  static const IconData usb;
  /// Bootstrap icon for boombox fill.
  static const IconData boomboxFill;
  /// Bootstrap icon for displayport.
  static const IconData displayport;
  /// Bootstrap icon for gpu card.
  static const IconData gpuCard;
  /// Bootstrap icon for memory.
  static const IconData memory;
  /// Bootstrap icon for modem fill.
  static const IconData modemFill;
  /// Bootstrap icon for modem.
  static const IconData modem;
  /// Bootstrap icon for motherboard fill.
  static const IconData motherboardFill;
  /// Bootstrap icon for motherboard.
  static const IconData motherboard;
  /// Bootstrap icon for optical audio fill.
  static const IconData opticalAudioFill;
  /// Bootstrap icon for optical audio.
  static const IconData opticalAudio;
  /// Bootstrap icon for pci card.
  static const IconData pciCard;
  /// Bootstrap icon for router fill.
  static const IconData routerFill;
  /// Bootstrap icon for router.
  static const IconData router;
  /// Bootstrap icon for thunderbolt fill.
  static const IconData thunderboltFill;
  /// Bootstrap icon for thunderbolt.
  static const IconData thunderbolt;
  /// Bootstrap icon for usb drive fill.
  static const IconData usbDriveFill;
  /// Bootstrap icon for usb drive.
  static const IconData usbDrive;
  /// Bootstrap icon for usb micro fill.
  static const IconData usbMicroFill;
  /// Bootstrap icon for usb micro.
  static const IconData usbMicro;
  /// Bootstrap icon for usb mini fill.
  static const IconData usbMiniFill;
  /// Bootstrap icon for usb mini.
  static const IconData usbMini;
  /// Bootstrap icon for cloud haze2.
  static const IconData cloudHaze2;
  /// Bootstrap icon for device hdd fill.
  static const IconData deviceHddFill;
  /// Bootstrap icon for device hdd.
  static const IconData deviceHdd;
  /// Bootstrap icon for device ssd fill.
  static const IconData deviceSsdFill;
  /// Bootstrap icon for device ssd.
  static const IconData deviceSsd;
  /// Bootstrap icon for displayport fill.
  static const IconData displayportFill;
  /// Bootstrap icon for mortarboard fill.
  static const IconData mortarboardFill;
  /// Bootstrap icon for mortarboard.
  static const IconData mortarboard;
  /// Bootstrap icon for terminal x.
  static const IconData terminalX;
  /// Bootstrap icon for arrow through heart fill.
  static const IconData arrowThroughHeartFill;
  /// Bootstrap icon for arrow through heart.
  static const IconData arrowThroughHeart;
  /// Bootstrap icon for badge sd fill.
  static const IconData badgeSdFill;
  /// Bootstrap icon for badge sd.
  static const IconData badgeSd;
  /// Bootstrap icon for bag heart fill.
  static const IconData bagHeartFill;
  /// Bootstrap icon for bag heart.
  static const IconData bagHeart;
  /// Bootstrap icon for balloon fill.
  static const IconData balloonFill;
  /// Bootstrap icon for balloon heart fill.
  static const IconData balloonHeartFill;
  /// Bootstrap icon for balloon heart.
  static const IconData balloonHeart;
  /// Bootstrap icon for balloon.
  static const IconData balloon;
  /// Bootstrap icon for box2 fill.
  static const IconData box2Fill;
  /// Bootstrap icon for box2 heart fill.
  static const IconData box2HeartFill;
  /// Bootstrap icon for box2 heart.
  static const IconData box2Heart;
  /// Bootstrap icon for box2.
  static const IconData box2;
  /// Bootstrap icon for braces asterisk.
  static const IconData bracesAsterisk;
  /// Bootstrap icon for calendar heart fill.
  static const IconData calendarHeartFill;
  /// Bootstrap icon for calendar heart.
  static const IconData calendarHeart;
  /// Bootstrap icon for calendar2 heart fill.
  static const IconData calendar2HeartFill;
  /// Bootstrap icon for calendar2 heart.
  static const IconData calendar2Heart;
  /// Bootstrap icon for chat heart fill.
  static const IconData chatHeartFill;
  /// Bootstrap icon for chat heart.
  static const IconData chatHeart;
  /// Bootstrap icon for chat left heart fill.
  static const IconData chatLeftHeartFill;
  /// Bootstrap icon for chat left heart.
  static const IconData chatLeftHeart;
  /// Bootstrap icon for chat right heart fill.
  static const IconData chatRightHeartFill;
  /// Bootstrap icon for chat right heart.
  static const IconData chatRightHeart;
  /// Bootstrap icon for chat square heart fill.
  static const IconData chatSquareHeartFill;
  /// Bootstrap icon for chat square heart.
  static const IconData chatSquareHeart;
  /// Bootstrap icon for clipboard check fill.
  static const IconData clipboardCheckFill;
  /// Bootstrap icon for clipboard data fill.
  static const IconData clipboardDataFill;
  /// Bootstrap icon for clipboard fill.
  static const IconData clipboardFill;
  /// Bootstrap icon for clipboard heart fill.
  static const IconData clipboardHeartFill;
  /// Bootstrap icon for clipboard heart.
  static const IconData clipboardHeart;
  /// Bootstrap icon for clipboard minus fill.
  static const IconData clipboardMinusFill;
  /// Bootstrap icon for clipboard plus fill.
  static const IconData clipboardPlusFill;
  /// Bootstrap icon for clipboard pulse.
  static const IconData clipboardPulse;
  /// Bootstrap icon for clipboard x fill.
  static const IconData clipboardXFill;
  /// Bootstrap icon for clipboard2 check fill.
  static const IconData clipboard2CheckFill;
  /// Bootstrap icon for clipboard2 check.
  static const IconData clipboard2Check;
  /// Bootstrap icon for clipboard2 data fill.
  static const IconData clipboard2DataFill;
  /// Bootstrap icon for clipboard2 data.
  static const IconData clipboard2Data;
  /// Bootstrap icon for clipboard2 fill.
  static const IconData clipboard2Fill;
  /// Bootstrap icon for clipboard2 heart fill.
  static const IconData clipboard2HeartFill;
  /// Bootstrap icon for clipboard2 heart.
  static const IconData clipboard2Heart;
  /// Bootstrap icon for clipboard2 minus fill.
  static const IconData clipboard2MinusFill;
  /// Bootstrap icon for clipboard2 minus.
  static const IconData clipboard2Minus;
  /// Bootstrap icon for clipboard2 plus fill.
  static const IconData clipboard2PlusFill;
  /// Bootstrap icon for clipboard2 plus.
  static const IconData clipboard2Plus;
  /// Bootstrap icon for clipboard2 pulse fill.
  static const IconData clipboard2PulseFill;
  /// Bootstrap icon for clipboard2 pulse.
  static const IconData clipboard2Pulse;
  /// Bootstrap icon for clipboard2 x fill.
  static const IconData clipboard2XFill;
  /// Bootstrap icon for clipboard2 x.
  static const IconData clipboard2X;
  /// Bootstrap icon for clipboard2.
  static const IconData clipboard2;
  /// Bootstrap icon for emoji kiss fill.
  static const IconData emojiKissFill;
  /// Bootstrap icon for emoji kiss.
  static const IconData emojiKiss;
  /// Bootstrap icon for envelope heart fill.
  static const IconData envelopeHeartFill;
  /// Bootstrap icon for envelope heart.
  static const IconData envelopeHeart;
  /// Bootstrap icon for envelope open heart fill.
  static const IconData envelopeOpenHeartFill;
  /// Bootstrap icon for envelope open heart.
  static const IconData envelopeOpenHeart;
  /// Bootstrap icon for envelope paper fill.
  static const IconData envelopePaperFill;
  /// Bootstrap icon for envelope paper heart fill.
  static const IconData envelopePaperHeartFill;
  /// Bootstrap icon for envelope paper heart.
  static const IconData envelopePaperHeart;
  /// Bootstrap icon for envelope paper.
  static const IconData envelopePaper;
  /// Bootstrap icon for filetype aac.
  static const IconData filetypeAac;
  /// Bootstrap icon for filetype ai.
  static const IconData filetypeAi;
  /// Bootstrap icon for filetype bmp.
  static const IconData filetypeBmp;
  /// Bootstrap icon for filetype cs.
  static const IconData filetypeCs;
  /// Bootstrap icon for filetype css.
  static const IconData filetypeCss;
  /// Bootstrap icon for filetype csv.
  static const IconData filetypeCsv;
  /// Bootstrap icon for filetype doc.
  static const IconData filetypeDoc;
  /// Bootstrap icon for filetype docx.
  static const IconData filetypeDocx;
  /// Bootstrap icon for filetype exe.
  static const IconData filetypeExe;
  /// Bootstrap icon for filetype gif.
  static const IconData filetypeGif;
  /// Bootstrap icon for filetype heic.
  static const IconData filetypeHeic;
  /// Bootstrap icon for filetype html.
  static const IconData filetypeHtml;
  /// Bootstrap icon for filetype java.
  static const IconData filetypeJava;
  /// Bootstrap icon for filetype jpg.
  static const IconData filetypeJpg;
  /// Bootstrap icon for filetype js.
  static const IconData filetypeJs;
  /// Bootstrap icon for filetype jsx.
  static const IconData filetypeJsx;
  /// Bootstrap icon for filetype key.
  static const IconData filetypeKey;
  /// Bootstrap icon for filetype m4p.
  static const IconData filetypeM4p;
  /// Bootstrap icon for filetype md.
  static const IconData filetypeMd;
  /// Bootstrap icon for filetype mdx.
  static const IconData filetypeMdx;
  /// Bootstrap icon for filetype mov.
  static const IconData filetypeMov;
  /// Bootstrap icon for filetype mp3.
  static const IconData filetypeMp3;
  /// Bootstrap icon for filetype mp4.
  static const IconData filetypeMp4;
  /// Bootstrap icon for filetype otf.
  static const IconData filetypeOtf;
  /// Bootstrap icon for filetype pdf.
  static const IconData filetypePdf;
  /// Bootstrap icon for filetype php.
  static const IconData filetypePhp;
  /// Bootstrap icon for filetype png.
  static const IconData filetypePng;
  /// Bootstrap icon for filetype ppt.
  static const IconData filetypePpt;
  /// Bootstrap icon for filetype psd.
  static const IconData filetypePsd;
  /// Bootstrap icon for filetype py.
  static const IconData filetypePy;
  /// Bootstrap icon for filetype raw.
  static const IconData filetypeRaw;
  /// Bootstrap icon for filetype rb.
  static const IconData filetypeRb;
  /// Bootstrap icon for filetype sass.
  static const IconData filetypeSass;
  /// Bootstrap icon for filetype scss.
  static const IconData filetypeScss;
  /// Bootstrap icon for filetype sh.
  static const IconData filetypeSh;
  /// Bootstrap icon for filetype svg.
  static const IconData filetypeSvg;
  /// Bootstrap icon for filetype tiff.
  static const IconData filetypeTiff;
  /// Bootstrap icon for filetype tsx.
  static const IconData filetypeTsx;
  /// Bootstrap icon for filetype ttf.
  static const IconData filetypeTtf;
  /// Bootstrap icon for filetype txt.
  static const IconData filetypeTxt;
  /// Bootstrap icon for filetype wav.
  static const IconData filetypeWav;
  /// Bootstrap icon for filetype woff.
  static const IconData filetypeWoff;
  /// Bootstrap icon for filetype xls.
  static const IconData filetypeXls;
  /// Bootstrap icon for filetype xml.
  static const IconData filetypeXml;
  /// Bootstrap icon for filetype yml.
  static const IconData filetypeYml;
  /// Bootstrap icon for heart arrow.
  static const IconData heartArrow;
  /// Bootstrap icon for heart pulse fill.
  static const IconData heartPulseFill;
  /// Bootstrap icon for heart pulse.
  static const IconData heartPulse;
  /// Bootstrap icon for heartbreak fill.
  static const IconData heartbreakFill;
  /// Bootstrap icon for heartbreak.
  static const IconData heartbreak;
  /// Bootstrap icon for hearts.
  static const IconData hearts;
  /// Bootstrap icon for hospital fill.
  static const IconData hospitalFill;
  /// Bootstrap icon for hospital.
  static const IconData hospital;
  /// Bootstrap icon for house heart fill.
  static const IconData houseHeartFill;
  /// Bootstrap icon for house heart.
  static const IconData houseHeart;
  /// Bootstrap icon for incognito.
  static const IconData incognito;
  /// Bootstrap icon for magnet fill.
  static const IconData magnetFill;
  /// Bootstrap icon for magnet.
  static const IconData magnet;
  /// Bootstrap icon for person heart.
  static const IconData personHeart;
  /// Bootstrap icon for person hearts.
  static const IconData personHearts;
  /// Bootstrap icon for phone flip.
  static const IconData phoneFlip;
  /// Bootstrap icon for plugin.
  static const IconData plugin;
  /// Bootstrap icon for postage fill.
  static const IconData postageFill;
  /// Bootstrap icon for postage heart fill.
  static const IconData postageHeartFill;
  /// Bootstrap icon for postage heart.
  static const IconData postageHeart;
  /// Bootstrap icon for postage.
  static const IconData postage;
  /// Bootstrap icon for postcard fill.
  static const IconData postcardFill;
  /// Bootstrap icon for postcard heart fill.
  static const IconData postcardHeartFill;
  /// Bootstrap icon for postcard heart.
  static const IconData postcardHeart;
  /// Bootstrap icon for postcard.
  static const IconData postcard;
  /// Bootstrap icon for search heart fill.
  static const IconData searchHeartFill;
  /// Bootstrap icon for search heart.
  static const IconData searchHeart;
  /// Bootstrap icon for sliders2 vertical.
  static const IconData sliders2Vertical;
  /// Bootstrap icon for sliders2.
  static const IconData sliders2;
  /// Bootstrap icon for trash3 fill.
  static const IconData trash3Fill;
  /// Bootstrap icon for trash3.
  static const IconData trash3;
  /// Bootstrap icon for valentine.
  static const IconData valentine;
  /// Bootstrap icon for valentine2.
  static const IconData valentine2;
  /// Bootstrap icon for wrench adjustable circle fill.
  static const IconData wrenchAdjustableCircleFill;
  /// Bootstrap icon for wrench adjustable circle.
  static const IconData wrenchAdjustableCircle;
  /// Bootstrap icon for wrench adjustable.
  static const IconData wrenchAdjustable;
  /// Bootstrap icon for filetype json.
  static const IconData filetypeJson;
  /// Bootstrap icon for filetype pptx.
  static const IconData filetypePptx;
  /// Bootstrap icon for filetype xlsx.
  static const IconData filetypeXlsx;
  /// Bootstrap icon for icon1 circle fill.
  static const IconData icon1CircleFill;
  /// Bootstrap icon for icon1 circle.
  static const IconData icon1Circle;
  /// Bootstrap icon for icon1 square fill.
  static const IconData icon1SquareFill;
  /// Bootstrap icon for icon1 square.
  static const IconData icon1Square;
  /// Bootstrap icon for icon2 circle fill.
  static const IconData icon2CircleFill;
  /// Bootstrap icon for icon2 circle.
  static const IconData icon2Circle;
  /// Bootstrap icon for icon2 square fill.
  static const IconData icon2SquareFill;
  /// Bootstrap icon for icon2 square.
  static const IconData icon2Square;
  /// Bootstrap icon for icon3 circle fill.
  static const IconData icon3CircleFill;
  /// Bootstrap icon for icon3 circle.
  static const IconData icon3Circle;
  /// Bootstrap icon for icon3 square fill.
  static const IconData icon3SquareFill;
  /// Bootstrap icon for icon3 square.
  static const IconData icon3Square;
  /// Bootstrap icon for icon4 circle fill.
  static const IconData icon4CircleFill;
  /// Bootstrap icon for icon4 circle.
  static const IconData icon4Circle;
  /// Bootstrap icon for icon4 square fill.
  static const IconData icon4SquareFill;
  /// Bootstrap icon for icon4 square.
  static const IconData icon4Square;
  /// Bootstrap icon for icon5 circle fill.
  static const IconData icon5CircleFill;
  /// Bootstrap icon for icon5 circle.
  static const IconData icon5Circle;
  /// Bootstrap icon for icon5 square fill.
  static const IconData icon5SquareFill;
  /// Bootstrap icon for icon5 square.
  static const IconData icon5Square;
  /// Bootstrap icon for icon6 circle fill.
  static const IconData icon6CircleFill;
  /// Bootstrap icon for icon6 circle.
  static const IconData icon6Circle;
  /// Bootstrap icon for icon6 square fill.
  static const IconData icon6SquareFill;
  /// Bootstrap icon for icon6 square.
  static const IconData icon6Square;
  /// Bootstrap icon for icon7 circle fill.
  static const IconData icon7CircleFill;
  /// Bootstrap icon for icon7 circle.
  static const IconData icon7Circle;
  /// Bootstrap icon for icon7 square fill.
  static const IconData icon7SquareFill;
  /// Bootstrap icon for icon7 square.
  static const IconData icon7Square;
  /// Bootstrap icon for icon8 circle fill.
  static const IconData icon8CircleFill;
  /// Bootstrap icon for icon8 circle.
  static const IconData icon8Circle;
  /// Bootstrap icon for icon8 square fill.
  static const IconData icon8SquareFill;
  /// Bootstrap icon for icon8 square.
  static const IconData icon8Square;
  /// Bootstrap icon for icon9 circle fill.
  static const IconData icon9CircleFill;
  /// Bootstrap icon for icon9 circle.
  static const IconData icon9Circle;
  /// Bootstrap icon for icon9 square fill.
  static const IconData icon9SquareFill;
  /// Bootstrap icon for icon9 square.
  static const IconData icon9Square;
  /// Bootstrap icon for airplane engines fill.
  static const IconData airplaneEnginesFill;
  /// Bootstrap icon for airplane engines.
  static const IconData airplaneEngines;
  /// Bootstrap icon for airplane fill.
  static const IconData airplaneFill;
  /// Bootstrap icon for airplane.
  static const IconData airplane;
  /// Bootstrap icon for alexa.
  static const IconData alexa;
  /// Bootstrap icon for alipay.
  static const IconData alipay;
  /// Bootstrap icon for android.
  static const IconData android;
  /// Bootstrap icon for android2.
  static const IconData android2;
  /// Bootstrap icon for box fill.
  static const IconData boxFill;
  /// Bootstrap icon for box seam fill.
  static const IconData boxSeamFill;
  /// Bootstrap icon for browser chrome.
  static const IconData browserChrome;
  /// Bootstrap icon for browser edge.
  static const IconData browserEdge;
  /// Bootstrap icon for browser firefox.
  static const IconData browserFirefox;
  /// Bootstrap icon for browser safari.
  static const IconData browserSafari;
  /// Bootstrap icon for c circle fill.
  static const IconData cCircleFill;
  /// Bootstrap icon for c circle.
  static const IconData cCircle;
  /// Bootstrap icon for c square fill.
  static const IconData cSquareFill;
  /// Bootstrap icon for c square.
  static const IconData cSquare;
  /// Bootstrap icon for capsule pill.
  static const IconData capsulePill;
  /// Bootstrap icon for capsule.
  static const IconData capsule;
  /// Bootstrap icon for car front fill.
  static const IconData carFrontFill;
  /// Bootstrap icon for car front.
  static const IconData carFront;
  /// Bootstrap icon for cassette fill.
  static const IconData cassetteFill;
  /// Bootstrap icon for cassette.
  static const IconData cassette;
  /// Bootstrap icon for cc circle fill.
  static const IconData ccCircleFill;
  /// Bootstrap icon for cc circle.
  static const IconData ccCircle;
  /// Bootstrap icon for cc square fill.
  static const IconData ccSquareFill;
  /// Bootstrap icon for cc square.
  static const IconData ccSquare;
  /// Bootstrap icon for cup hot fill.
  static const IconData cupHotFill;
  /// Bootstrap icon for cup hot.
  static const IconData cupHot;
  /// Bootstrap icon for currency rupee.
  static const IconData currencyRupee;
  /// Bootstrap icon for dropbox.
  static const IconData dropbox;
  /// Bootstrap icon for escape.
  static const IconData escape;
  /// Bootstrap icon for fast forward btn fill.
  static const IconData fastForwardBtnFill;
  /// Bootstrap icon for fast forward btn.
  static const IconData fastForwardBtn;
  /// Bootstrap icon for fast forward circle fill.
  static const IconData fastForwardCircleFill;
  /// Bootstrap icon for fast forward circle.
  static const IconData fastForwardCircle;
  /// Bootstrap icon for fast forward fill.
  static const IconData fastForwardFill;
  /// Bootstrap icon for fast forward.
  static const IconData fastForward;
  /// Bootstrap icon for filetype sql.
  static const IconData filetypeSql;
  /// Bootstrap icon for fire.
  static const IconData fire;
  /// Bootstrap icon for google play.
  static const IconData googlePlay;
  /// Bootstrap icon for h circle fill.
  static const IconData hCircleFill;
  /// Bootstrap icon for h circle.
  static const IconData hCircle;
  /// Bootstrap icon for h square fill.
  static const IconData hSquareFill;
  /// Bootstrap icon for h square.
  static const IconData hSquare;
  /// Bootstrap icon for indent.
  static const IconData indent;
  /// Bootstrap icon for lungs fill.
  static const IconData lungsFill;
  /// Bootstrap icon for lungs.
  static const IconData lungs;
  /// Bootstrap icon for microsoft teams.
  static const IconData microsoftTeams;
  /// Bootstrap icon for p circle fill.
  static const IconData pCircleFill;
  /// Bootstrap icon for p circle.
  static const IconData pCircle;
  /// Bootstrap icon for p square fill.
  static const IconData pSquareFill;
  /// Bootstrap icon for p square.
  static const IconData pSquare;
  /// Bootstrap icon for pass fill.
  static const IconData passFill;
  /// Bootstrap icon for pass.
  static const IconData pass;
  /// Bootstrap icon for prescription.
  static const IconData prescription;
  /// Bootstrap icon for prescription2.
  static const IconData prescription2;
  /// Bootstrap icon for r circle fill.
  static const IconData rCircleFill;
  /// Bootstrap icon for r circle.
  static const IconData rCircle;
  /// Bootstrap icon for r square fill.
  static const IconData rSquareFill;
  /// Bootstrap icon for r square.
  static const IconData rSquare;
  /// Bootstrap icon for repeat1.
  static const IconData repeat1;
  /// Bootstrap icon for repeat.
  static const IconData repeat;
  /// Bootstrap icon for rewind btn fill.
  static const IconData rewindBtnFill;
  /// Bootstrap icon for rewind btn.
  static const IconData rewindBtn;
  /// Bootstrap icon for rewind circle fill.
  static const IconData rewindCircleFill;
  /// Bootstrap icon for rewind circle.
  static const IconData rewindCircle;
  /// Bootstrap icon for rewind fill.
  static const IconData rewindFill;
  /// Bootstrap icon for rewind.
  static const IconData rewind;
  /// Bootstrap icon for train freight front fill.
  static const IconData trainFreightFrontFill;
  /// Bootstrap icon for train freight front.
  static const IconData trainFreightFront;
  /// Bootstrap icon for train front fill.
  static const IconData trainFrontFill;
  /// Bootstrap icon for train front.
  static const IconData trainFront;
  /// Bootstrap icon for train lightrail front fill.
  static const IconData trainLightrailFrontFill;
  /// Bootstrap icon for train lightrail front.
  static const IconData trainLightrailFront;
  /// Bootstrap icon for truck front fill.
  static const IconData truckFrontFill;
  /// Bootstrap icon for truck front.
  static const IconData truckFront;
  /// Bootstrap icon for ubuntu.
  static const IconData ubuntu;
  /// Bootstrap icon for unindent.
  static const IconData unindent;
  /// Bootstrap icon for unity.
  static const IconData unity;
  /// Bootstrap icon for universal access circle.
  static const IconData universalAccessCircle;
  /// Bootstrap icon for universal access.
  static const IconData universalAccess;
  /// Bootstrap icon for virus.
  static const IconData virus;
  /// Bootstrap icon for virus2.
  static const IconData virus2;
  /// Bootstrap icon for wechat.
  static const IconData wechat;
  /// Bootstrap icon for yelp.
  static const IconData yelp;
  /// Bootstrap icon for sign stop fill.
  static const IconData signStopFill;
  /// Bootstrap icon for sign stop lights fill.
  static const IconData signStopLightsFill;
  /// Bootstrap icon for sign stop lights.
  static const IconData signStopLights;
  /// Bootstrap icon for sign stop.
  static const IconData signStop;
  /// Bootstrap icon for sign turn left fill.
  static const IconData signTurnLeftFill;
  /// Bootstrap icon for sign turn left.
  static const IconData signTurnLeft;
  /// Bootstrap icon for sign turn right fill.
  static const IconData signTurnRightFill;
  /// Bootstrap icon for sign turn right.
  static const IconData signTurnRight;
  /// Bootstrap icon for sign turn slight left fill.
  static const IconData signTurnSlightLeftFill;
  /// Bootstrap icon for sign turn slight left.
  static const IconData signTurnSlightLeft;
  /// Bootstrap icon for sign turn slight right fill.
  static const IconData signTurnSlightRightFill;
  /// Bootstrap icon for sign turn slight right.
  static const IconData signTurnSlightRight;
  /// Bootstrap icon for sign yield fill.
  static const IconData signYieldFill;
  /// Bootstrap icon for sign yield.
  static const IconData signYield;
  /// Bootstrap icon for ev station fill.
  static const IconData evStationFill;
  /// Bootstrap icon for ev station.
  static const IconData evStation;
  /// Bootstrap icon for fuel pump diesel fill.
  static const IconData fuelPumpDieselFill;
  /// Bootstrap icon for fuel pump diesel.
  static const IconData fuelPumpDiesel;
  /// Bootstrap icon for fuel pump fill.
  static const IconData fuelPumpFill;
  /// Bootstrap icon for fuel pump.
  static const IconData fuelPump;
  /// Bootstrap icon for icon0 circle fill.
  static const IconData icon0CircleFill;
  /// Bootstrap icon for icon0 circle.
  static const IconData icon0Circle;
  /// Bootstrap icon for icon0 square fill.
  static const IconData icon0SquareFill;
  /// Bootstrap icon for icon0 square.
  static const IconData icon0Square;
  /// Bootstrap icon for rocket fill.
  static const IconData rocketFill;
  /// Bootstrap icon for rocket takeoff fill.
  static const IconData rocketTakeoffFill;
  /// Bootstrap icon for rocket takeoff.
  static const IconData rocketTakeoff;
  /// Bootstrap icon for rocket.
  static const IconData rocket;
  /// Bootstrap icon for stripe.
  static const IconData stripe;
  /// Bootstrap icon for subscript.
  static const IconData subscript;
  /// Bootstrap icon for superscript.
  static const IconData superscript;
  /// Bootstrap icon for trello.
  static const IconData trello;
  /// Bootstrap icon for envelope at fill.
  static const IconData envelopeAtFill;
  /// Bootstrap icon for envelope at.
  static const IconData envelopeAt;
  /// Bootstrap icon for regex.
  static const IconData regex;
  /// Bootstrap icon for text wrap.
  static const IconData textWrap;
  /// Bootstrap icon for sign dead end fill.
  static const IconData signDeadEndFill;
  /// Bootstrap icon for sign dead end.
  static const IconData signDeadEnd;
  /// Bootstrap icon for sign do not enter fill.
  static const IconData signDoNotEnterFill;
  /// Bootstrap icon for sign do not enter.
  static const IconData signDoNotEnter;
  /// Bootstrap icon for sign intersection fill.
  static const IconData signIntersectionFill;
  /// Bootstrap icon for sign intersection side fill.
  static const IconData signIntersectionSideFill;
  /// Bootstrap icon for sign intersection side.
  static const IconData signIntersectionSide;
  /// Bootstrap icon for sign intersection t fill.
  static const IconData signIntersectionTFill;
  /// Bootstrap icon for sign intersection t.
  static const IconData signIntersectionT;
  /// Bootstrap icon for sign intersection y fill.
  static const IconData signIntersectionYFill;
  /// Bootstrap icon for sign intersection y.
  static const IconData signIntersectionY;
  /// Bootstrap icon for sign intersection.
  static const IconData signIntersection;
  /// Bootstrap icon for sign merge left fill.
  static const IconData signMergeLeftFill;
  /// Bootstrap icon for sign merge left.
  static const IconData signMergeLeft;
  /// Bootstrap icon for sign merge right fill.
  static const IconData signMergeRightFill;
  /// Bootstrap icon for sign merge right.
  static const IconData signMergeRight;
  /// Bootstrap icon for sign no left turn fill.
  static const IconData signNoLeftTurnFill;
  /// Bootstrap icon for sign no left turn.
  static const IconData signNoLeftTurn;
  /// Bootstrap icon for sign no parking fill.
  static const IconData signNoParkingFill;
  /// Bootstrap icon for sign no parking.
  static const IconData signNoParking;
  /// Bootstrap icon for sign no right turn fill.
  static const IconData signNoRightTurnFill;
  /// Bootstrap icon for sign no right turn.
  static const IconData signNoRightTurn;
  /// Bootstrap icon for sign railroad fill.
  static const IconData signRailroadFill;
  /// Bootstrap icon for sign railroad.
  static const IconData signRailroad;
  /// Bootstrap icon for building add.
  static const IconData buildingAdd;
  /// Bootstrap icon for building check.
  static const IconData buildingCheck;
  /// Bootstrap icon for building dash.
  static const IconData buildingDash;
  /// Bootstrap icon for building down.
  static const IconData buildingDown;
  /// Bootstrap icon for building exclamation.
  static const IconData buildingExclamation;
  /// Bootstrap icon for building fill add.
  static const IconData buildingFillAdd;
  /// Bootstrap icon for building fill check.
  static const IconData buildingFillCheck;
  /// Bootstrap icon for building fill dash.
  static const IconData buildingFillDash;
  /// Bootstrap icon for building fill down.
  static const IconData buildingFillDown;
  /// Bootstrap icon for building fill exclamation.
  static const IconData buildingFillExclamation;
  /// Bootstrap icon for building fill gear.
  static const IconData buildingFillGear;
  /// Bootstrap icon for building fill lock.
  static const IconData buildingFillLock;
  /// Bootstrap icon for building fill slash.
  static const IconData buildingFillSlash;
  /// Bootstrap icon for building fill up.
  static const IconData buildingFillUp;
  /// Bootstrap icon for building fill x.
  static const IconData buildingFillX;
  /// Bootstrap icon for building fill.
  static const IconData buildingFill;
  /// Bootstrap icon for building gear.
  static const IconData buildingGear;
  /// Bootstrap icon for building lock.
  static const IconData buildingLock;
  /// Bootstrap icon for building slash.
  static const IconData buildingSlash;
  /// Bootstrap icon for building up.
  static const IconData buildingUp;
  /// Bootstrap icon for building x.
  static const IconData buildingX;
  /// Bootstrap icon for buildings fill.
  static const IconData buildingsFill;
  /// Bootstrap icon for buildings.
  static const IconData buildings;
  /// Bootstrap icon for bus front fill.
  static const IconData busFrontFill;
  /// Bootstrap icon for bus front.
  static const IconData busFront;
  /// Bootstrap icon for ev front fill.
  static const IconData evFrontFill;
  /// Bootstrap icon for ev front.
  static const IconData evFront;
  /// Bootstrap icon for globe americas.
  static const IconData globeAmericas;
  /// Bootstrap icon for globe asia australia.
  static const IconData globeAsiaAustralia;
  /// Bootstrap icon for globe central south asia.
  static const IconData globeCentralSouthAsia;
  /// Bootstrap icon for globe europe africa.
  static const IconData globeEuropeAfrica;
  /// Bootstrap icon for house add fill.
  static const IconData houseAddFill;
  /// Bootstrap icon for house add.
  static const IconData houseAdd;
  /// Bootstrap icon for house check fill.
  static const IconData houseCheckFill;
  /// Bootstrap icon for house check.
  static const IconData houseCheck;
  /// Bootstrap icon for house dash fill.
  static const IconData houseDashFill;
  /// Bootstrap icon for house dash.
  static const IconData houseDash;
  /// Bootstrap icon for house down fill.
  static const IconData houseDownFill;
  /// Bootstrap icon for house down.
  static const IconData houseDown;
  /// Bootstrap icon for house exclamation fill.
  static const IconData houseExclamationFill;
  /// Bootstrap icon for house exclamation.
  static const IconData houseExclamation;
  /// Bootstrap icon for house gear fill.
  static const IconData houseGearFill;
  /// Bootstrap icon for house gear.
  static const IconData houseGear;
  /// Bootstrap icon for house lock fill.
  static const IconData houseLockFill;
  /// Bootstrap icon for house lock.
  static const IconData houseLock;
  /// Bootstrap icon for house slash fill.
  static const IconData houseSlashFill;
  /// Bootstrap icon for house slash.
  static const IconData houseSlash;
  /// Bootstrap icon for house up fill.
  static const IconData houseUpFill;
  /// Bootstrap icon for house up.
  static const IconData houseUp;
  /// Bootstrap icon for house x fill.
  static const IconData houseXFill;
  /// Bootstrap icon for house x.
  static const IconData houseX;
  /// Bootstrap icon for person add.
  static const IconData personAdd;
  /// Bootstrap icon for person down.
  static const IconData personDown;
  /// Bootstrap icon for person exclamation.
  static const IconData personExclamation;
  /// Bootstrap icon for person fill add.
  static const IconData personFillAdd;
  /// Bootstrap icon for person fill check.
  static const IconData personFillCheck;
  /// Bootstrap icon for person fill dash.
  static const IconData personFillDash;
  /// Bootstrap icon for person fill down.
  static const IconData personFillDown;
  /// Bootstrap icon for person fill exclamation.
  static const IconData personFillExclamation;
  /// Bootstrap icon for person fill gear.
  static const IconData personFillGear;
  /// Bootstrap icon for person fill lock.
  static const IconData personFillLock;
  /// Bootstrap icon for person fill slash.
  static const IconData personFillSlash;
  /// Bootstrap icon for person fill up.
  static const IconData personFillUp;
  /// Bootstrap icon for person fill x.
  static const IconData personFillX;
  /// Bootstrap icon for person gear.
  static const IconData personGear;
  /// Bootstrap icon for person lock.
  static const IconData personLock;
  /// Bootstrap icon for person slash.
  static const IconData personSlash;
  /// Bootstrap icon for person up.
  static const IconData personUp;
  /// Bootstrap icon for scooter.
  static const IconData scooter;
  /// Bootstrap icon for taxi front fill.
  static const IconData taxiFrontFill;
  /// Bootstrap icon for taxi front.
  static const IconData taxiFront;
  /// Bootstrap icon for amd.
  static const IconData amd;
  /// Bootstrap icon for database add.
  static const IconData databaseAdd;
  /// Bootstrap icon for database check.
  static const IconData databaseCheck;
  /// Bootstrap icon for database dash.
  static const IconData databaseDash;
  /// Bootstrap icon for database down.
  static const IconData databaseDown;
  /// Bootstrap icon for database exclamation.
  static const IconData databaseExclamation;
  /// Bootstrap icon for database fill add.
  static const IconData databaseFillAdd;
  /// Bootstrap icon for database fill check.
  static const IconData databaseFillCheck;
  /// Bootstrap icon for database fill dash.
  static const IconData databaseFillDash;
  /// Bootstrap icon for database fill down.
  static const IconData databaseFillDown;
  /// Bootstrap icon for database fill exclamation.
  static const IconData databaseFillExclamation;
  /// Bootstrap icon for database fill gear.
  static const IconData databaseFillGear;
  /// Bootstrap icon for database fill lock.
  static const IconData databaseFillLock;
  /// Bootstrap icon for database fill slash.
  static const IconData databaseFillSlash;
  /// Bootstrap icon for database fill up.
  static const IconData databaseFillUp;
  /// Bootstrap icon for database fill x.
  static const IconData databaseFillX;
  /// Bootstrap icon for database fill.
  static const IconData databaseFill;
  /// Bootstrap icon for database gear.
  static const IconData databaseGear;
  /// Bootstrap icon for database lock.
  static const IconData databaseLock;
  /// Bootstrap icon for database slash.
  static const IconData databaseSlash;
  /// Bootstrap icon for database up.
  static const IconData databaseUp;
  /// Bootstrap icon for database x.
  static const IconData databaseX;
  /// Bootstrap icon for database.
  static const IconData database;
  /// Bootstrap icon for houses fill.
  static const IconData housesFill;
  /// Bootstrap icon for houses.
  static const IconData houses;
  /// Bootstrap icon for nvidia.
  static const IconData nvidia;
  /// Bootstrap icon for person vcard fill.
  static const IconData personVcardFill;
  /// Bootstrap icon for person vcard.
  static const IconData personVcard;
  /// Bootstrap icon for sina weibo.
  static const IconData sinaWeibo;
  /// Bootstrap icon for tencent qq.
  static const IconData tencentQq;
  /// Bootstrap icon for wikipedia.
  static const IconData wikipedia;
  /// Bootstrap icon for alphabet uppercase.
  static const IconData alphabetUppercase;
  /// Bootstrap icon for alphabet.
  static const IconData alphabet;
  /// Bootstrap icon for amazon.
  static const IconData amazon;
  /// Bootstrap icon for arrows collapse vertical.
  static const IconData arrowsCollapseVertical;
  /// Bootstrap icon for arrows expand vertical.
  static const IconData arrowsExpandVertical;
  /// Bootstrap icon for arrows vertical.
  static const IconData arrowsVertical;
  /// Bootstrap icon for arrows.
  static const IconData arrows;
  /// Bootstrap icon for ban fill.
  static const IconData banFill;
  /// Bootstrap icon for ban.
  static const IconData ban;
  /// Bootstrap icon for bing.
  static const IconData bing;
  /// Bootstrap icon for cake.
  static const IconData cake;
  /// Bootstrap icon for cake2.
  static const IconData cake2;
  /// Bootstrap icon for cookie.
  static const IconData cookie;
  /// Bootstrap icon for copy.
  static const IconData copy;
  /// Bootstrap icon for crosshair.
  static const IconData crosshair;
  /// Bootstrap icon for crosshair2.
  static const IconData crosshair2;
  /// Bootstrap icon for emoji astonished fill.
  static const IconData emojiAstonishedFill;
  /// Bootstrap icon for emoji astonished.
  static const IconData emojiAstonished;
  /// Bootstrap icon for emoji grimace fill.
  static const IconData emojiGrimaceFill;
  /// Bootstrap icon for emoji grimace.
  static const IconData emojiGrimace;
  /// Bootstrap icon for emoji grin fill.
  static const IconData emojiGrinFill;
  /// Bootstrap icon for emoji grin.
  static const IconData emojiGrin;
  /// Bootstrap icon for emoji surprise fill.
  static const IconData emojiSurpriseFill;
  /// Bootstrap icon for emoji surprise.
  static const IconData emojiSurprise;
  /// Bootstrap icon for emoji tear fill.
  static const IconData emojiTearFill;
  /// Bootstrap icon for emoji tear.
  static const IconData emojiTear;
  /// Bootstrap icon for envelope arrow down fill.
  static const IconData envelopeArrowDownFill;
  /// Bootstrap icon for envelope arrow down.
  static const IconData envelopeArrowDown;
  /// Bootstrap icon for envelope arrow up fill.
  static const IconData envelopeArrowUpFill;
  /// Bootstrap icon for envelope arrow up.
  static const IconData envelopeArrowUp;
  /// Bootstrap icon for feather.
  static const IconData feather;
  /// Bootstrap icon for feather2.
  static const IconData feather2;
  /// Bootstrap icon for floppy fill.
  static const IconData floppyFill;
  /// Bootstrap icon for floppy.
  static const IconData floppy;
  /// Bootstrap icon for floppy2 fill.
  static const IconData floppy2Fill;
  /// Bootstrap icon for floppy2.
  static const IconData floppy2;
  /// Bootstrap icon for gitlab.
  static const IconData gitlab;
  /// Bootstrap icon for highlighter.
  static const IconData highlighter;
  /// Bootstrap icon for marker tip.
  static const IconData markerTip;
  /// Bootstrap icon for nvme fill.
  static const IconData nvmeFill;
  /// Bootstrap icon for nvme.
  static const IconData nvme;
  /// Bootstrap icon for opencollective.
  static const IconData opencollective;
  /// Bootstrap icon for pci card network.
  static const IconData pciCardNetwork;
  /// Bootstrap icon for pci card sound.
  static const IconData pciCardSound;
  /// Bootstrap icon for radar.
  static const IconData radar;
  /// Bootstrap icon for send arrow down fill.
  static const IconData sendArrowDownFill;
  /// Bootstrap icon for send arrow down.
  static const IconData sendArrowDown;
  /// Bootstrap icon for send arrow up fill.
  static const IconData sendArrowUpFill;
  /// Bootstrap icon for send arrow up.
  static const IconData sendArrowUp;
  /// Bootstrap icon for sim slash fill.
  static const IconData simSlashFill;
  /// Bootstrap icon for sim slash.
  static const IconData simSlash;
  /// Bootstrap icon for sourceforge.
  static const IconData sourceforge;
  /// Bootstrap icon for substack.
  static const IconData substack;
  /// Bootstrap icon for threads fill.
  static const IconData threadsFill;
  /// Bootstrap icon for threads.
  static const IconData threads;
  /// Bootstrap icon for transparency.
  static const IconData transparency;
  /// Bootstrap icon for twitter x.
  static const IconData twitterX;
  /// Bootstrap icon for type h4.
  static const IconData typeH4;
  /// Bootstrap icon for type h5.
  static const IconData typeH5;
  /// Bootstrap icon for type h6.
  static const IconData typeH6;
  /// Bootstrap icon for backpack fill.
  static const IconData backpackFill;
  /// Bootstrap icon for backpack.
  static const IconData backpack;
  /// Bootstrap icon for backpack2 fill.
  static const IconData backpack2Fill;
  /// Bootstrap icon for backpack2.
  static const IconData backpack2;
  /// Bootstrap icon for backpack3 fill.
  static const IconData backpack3Fill;
  /// Bootstrap icon for backpack3.
  static const IconData backpack3;
  /// Bootstrap icon for backpack4 fill.
  static const IconData backpack4Fill;
  /// Bootstrap icon for backpack4.
  static const IconData backpack4;
  /// Bootstrap icon for brilliance.
  static const IconData brilliance;
  /// Bootstrap icon for cake fill.
  static const IconData cakeFill;
  /// Bootstrap icon for cake2 fill.
  static const IconData cake2Fill;
  /// Bootstrap icon for duffle fill.
  static const IconData duffleFill;
  /// Bootstrap icon for duffle.
  static const IconData duffle;
  /// Bootstrap icon for exposure.
  static const IconData exposure;
  /// Bootstrap icon for gender neuter.
  static const IconData genderNeuter;
  /// Bootstrap icon for highlights.
  static const IconData highlights;
  /// Bootstrap icon for luggage fill.
  static const IconData luggageFill;
  /// Bootstrap icon for luggage.
  static const IconData luggage;
  /// Bootstrap icon for mailbox flag.
  static const IconData mailboxFlag;
  /// Bootstrap icon for mailbox2 flag.
  static const IconData mailbox2Flag;
  /// Bootstrap icon for noise reduction.
  static const IconData noiseReduction;
  /// Bootstrap icon for passport fill.
  static const IconData passportFill;
  /// Bootstrap icon for passport.
  static const IconData passport;
  /// Bootstrap icon for person arms up.
  static const IconData personArmsUp;
  /// Bootstrap icon for person raised hand.
  static const IconData personRaisedHand;
  /// Bootstrap icon for person standing dress.
  static const IconData personStandingDress;
  /// Bootstrap icon for person standing.
  static const IconData personStanding;
  /// Bootstrap icon for person walking.
  static const IconData personWalking;
  /// Bootstrap icon for person wheelchair.
  static const IconData personWheelchair;
  /// Bootstrap icon for shadows.
  static const IconData shadows;
  /// Bootstrap icon for suitcase fill.
  static const IconData suitcaseFill;
  /// Bootstrap icon for suitcase lg fill.
  static const IconData suitcaseLgFill;
  /// Bootstrap icon for suitcase lg.
  static const IconData suitcaseLg;
  /// Bootstrap icon for suitcase.
  static const IconData suitcase;
  /// Bootstrap icon for suitcase2 fill.
  static const IconData suitcase2Fill;
  /// Bootstrap icon for suitcase2.
  static const IconData suitcase2;
  /// Bootstrap icon for vignette.
  static const IconData vignette;
}
```
